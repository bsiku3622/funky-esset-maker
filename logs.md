# 작업 기록

## 2026-08-13 — AI Figure Maker: 여러 개 선택이 MLP 간선 하나로 무너지던 버그

- 변경 파일: `src/tools/AiFigureMaker.tsx`
- 요약: 마퀴로 여러 개를 고른 뒤 누르면 MLP 시냅스 하나만 남던 문제. 부분 진입에 조건 두 개를 걸었습니다.

### 조건이 하나 모자랐다

부분 진입 조건이 `already`(그 노드가 선택되어 있음)뿐이었습니다. 그런데 마퀴로 MLP + 다른 도형 셋을 잡은 상태에서도 `already`는 참이라, **옮기려고 누른 순간 시냅스 하나로 선택이 무너졌습니다.** 시냅스는 상자 전체를 가로지르니 어디를 눌러도 거의 걸립니다.

⚠️ **조건 두 개가 필요했고, 둘 다 이유가 다릅니다.**

**하나 — 그 노드가 유일한 선택일 때만.** 여러 개를 고른 상태에서 하나의 내부로 들어가는 건 어느 편집기에서도 말이 안 됩니다.

**둘 — 누를 때가 아니라 뗄 때 결정.** 이게 더 미묘합니다. 누르는 순간 판정하면 조건 하나만으로도 **선택된 MLP를 옮길 수가 없습니다** — 시냅스가 상자의 대부분을 덮으니 잡을 빈자리가 거의 없거든요. 그래서 누를 땐 후보만 `drag.part`에 담아 두고 평소처럼 move 드래그를 시작하고, **드래그가 일어나지 않았을 때만**(`!drag.moved`) 부분 선택으로 처리합니다. 클릭은 선택, 드래그는 이동.

## 2026-08-13 — AI Figure Maker: 캐럿이 커서를 따라가게

- 변경 파일: `src/tools/aifig/layout.ts`, `latex.ts`, `AiFigureMaker.tsx`, `latex.test.ts`
- 요약: 그려지는 캐럿이 텍스트 커서 위치를 따라갑니다. 이전엔 항상 글자 끝에 붙어 있어서 ←를 눌러도 안 움직였습니다.

### 접두사를 조판해서 재면 된다

상자를 없앤 대가로 "캐럿은 글자 끝에 선다"를 받아들였는데, 실제로 쓰면 **←가 안 먹는 것처럼 보입니다.** 커서는 움직이는데 화면은 그대로고, 그 상태로 타이핑하면 글자가 엉뚱한 데 들어갑니다.

풀 수 있었습니다. **커서 앞까지의 소스를 따로 조판해서 그 폭을 재면** 됩니다. 산문에선 소스와 글자가 1:1이라 정확하고, 수식에서도 선형 수식의 접두사는 실제 폭을 가집니다 — `\sigma(Wx`는 σ(Wx만큼입니다.

⚠️ **명령 중간에서 잘린 접두사는 TeX가 아닙니다.** `x_1`을 지나가는 `x_`는 문법 오류라 폭이 없습니다. 그때는 **한 글자씩 뒤로 물러나며 조판되는 마지막 접두사**를 찾습니다 — `x_` → `x`. 캐럿이 x 뒤에 서고, 커서가 명령 안에 있을 때마다 끝으로 튀는 일이 없어집니다.

정확히 안 되는 경우는 남습니다: 워드랩된 라벨(소스에 없는 줄을 레이아웃이 만들어내므로 끝으로 폴백), 그리고 `\frac{a}{b}`처럼 선형이 아닌 구조의 내부.

### 곁가지: `textWidth`의 폴백은 한 번도 도달한 적이 없다

이 테스트를 쓰다가 나왔습니다. `textWidth`는 캔버스가 없으면 `text.length * 7`로 물러나게 되어 있는데, 그 전에 `measureCtx`가 `document.createElement`에서 **던집니다.** DOM 없는 환경에선 전부 예외였고, 그래서 이 코드가 테스트에서 한 번도 안 돌았습니다. `inkExtent`는 이미 `typeof document` 검사를 하고 있었는데 여기만 빠져 있었습니다.

## 2026-08-13 — AI Figure Maker: 라벨 편집에서도 상자를 없애고, 뉴런 포커스를 진짜 단독 선택으로

- 변경 파일: `src/tools/aifig/layout.ts`, `mlp.ts`, `presets.ts`, `AiFigureMaker.tsx`, `AiFigureMaker.css`, `MathInput.tsx`(삭제)
- 요약: 노드 라벨도 원 안 타이핑과 같은 방식(투명 입력 + 캐럿)으로 통일했습니다. 뉴런 포커스 시 MLP 전체 선택 chrome을 숨깁니다. MLP 기본값을 [4,64,64,64,1] · pitch 48 · gap 56 · r 16 · 생략 4로.

### math-field를 걷어냈다

뉴런은 원 안에서 바로 써지는데 **노드 라벨만 여전히 상자**였습니다 — LaTeX 모드는 `math-field`(크림 배경 + 보라 테두리), 나머지는 textarea. 편집기가 두 종류였고 둘 다 그림 위에 상자를 얹었죠.

이제 한 종류입니다. 투명 textarea가 키를 받고, **도형 렌더러가 라벨을 그리고**, 오버레이가 캐럿을 그립니다. `MathInput.tsx`와 `.af-mathedit`/`.af-edit` 스타일은 지웠습니다. MathLive는 이제 aifig에서 안 쓰지만 Cartesian Plotter가 쓰므로 패키지는 남습니다 — 대신 AI Figure Maker 번들에서 800 kB가 완전히 빠졌습니다.

⚠️ **대가가 있습니다.**
- 캐럿은 커서 **위치를 따라가되 근사**입니다 — 아래 "커서를 따라가게" 참고.
- **선택 영역이 안 보입니다.** 글자가 transparent라 드래그 선택이나 ⌘A가 화면에 안 나타납니다. 동작은 합니다.

상자 편집기는 정확히 반대 거래를 하고 있었습니다 — 소스는 정확히 보여주고 결과는 전혀 안 보여주는. 어느 쪽이 나은지는 취향이 아니라 용도의 문제고, 여기선 결과가 이깁니다.

편집을 열면 캐럿을 **문자열 끝**으로 보냅니다(`setSelectionRange`). 안 하면 새 글자가 기존 라벨 **앞**에 붙습니다 — `PDE`를 고치려다 `LossPDE`가 됩니다.

### 뉴런 포커스는 단독 선택이다

뉴런에 링을 그리면서 MLP 전체의 외곽선·리사이즈 그립·회전 손잡이를 그대로 두고 있었습니다. 두 개가 동시에 선택된 것처럼 읽히고, **원 하나 주위에 격자 전체를 리사이즈하는 핸들**이 놓입니다. 부분이 포커스되면 노드 chrome을 통째로 내립니다(hover 앵커도 함께).

### 뉴런이 자기 글꼴 모드를 가진다

라벨 편집 상자를 없애면서 뉴런 포커스 시 노드 패널을 내렸는데, **뉴런 텍스트가 노드의 글꼴 모드를 따르는데 정작 그 셀렉트에 손이 안 닿는** 상태가 됐습니다. 만든 쪽 실수입니다.

노드 셀렉트를 뉴런 패널에도 복제하는 대신 `NeuronBits.fontFamily`를 뒀습니다. 없으면 네트워크를 따르고, 있으면 그 원만 다르게 읽습니다 — 기호가 늘어선 열에 "bias" 하나가 산문으로 섞이는 경우가 실제로 있습니다. 드롭다운의 기본 항목은 "네트워크를 따름"이고, 그걸 다시 고르면 **오늘 값을 굳히는 게 아니라 오버라이드를 지웁니다.**

모드를 바꿀 때 `retypeLabel`을 태우는 것도 노드와 같습니다. 안 하면 셀렉트 한 번에 `$x_1$`이 TeX 에러가 됩니다.

### 기본값

`[4, 64, 64, 64, 1]`, pitch 48, gap 56, r 16, 생략 4 → 박스 256×224. 4유닛 층과 64유닛 층이 한 그림에 같이 나와서 생략(⋮)이 무슨 일을 하는지 처음 만들자마자 보입니다. ⚠️ 박스 크기는 격자에서 재계산되므로 카탈로그의 `w/h`가 어긋나면 처음 만질 때 도형이 튑니다 — 셋 중 하나를 바꾸면 같이 고쳐야 합니다.

## 2026-08-13 — AI Figure Maker: 뉴런을 원 안에서 직접 편집하고, 포커스되면 뉴런만 다루게

- 변경 파일: `src/tools/aifig/latex.ts`, `layout.ts`, `shapes.tsx`, `doc.ts`, `mlp.ts`, `presets.ts`, `Inspector.tsx`, `Overlay.tsx`, `AiFigureMaker.tsx`, `AiFigureMaker.css`, `mlp.test.ts`
- 요약: 원을 더블클릭하면 그 원 안에서 바로 타이핑됩니다. 뉴런이 포커스되면 우측 패널이 뉴런 것만 보여줍니다. 뉴런 기본 반지름 8 → 16.

### 편집기가 아니라 캐럿만 남겼다

더블클릭이 뉴런이 아니라 **네트워크 노드의 라벨**을 열고 있었고, 그 편집기는 노드 크기의 상자라 그림 위를 가로로 덮었습니다. 가리킨 것과 열린 것이 달랐죠.

이제 원 위에 **완전히 투명한 `<input>`** 을 올립니다 — 글자색·배경·테두리·자체 캐럿까지 전부 transparent. 키를 받고 IME가 조합할 자리가 필요해서 존재할 뿐이고, 글자는 도형 렌더러가 이미 그리고 있습니다. 화면에 남는 chrome은 오버레이가 그리는 **캐럿 한 줄**뿐입니다.

⚠️ `.af-inplace`에 배경이나 테두리를 주면 그 순간 이게 대체한 그 상자로 돌아갑니다.

캐럿을 글자 뒤에 놓으려면 라벨 폭을 알아야 하는데, 그건 렌더러만 알고 있었습니다. `neuronLabel`/`neuronLabelStyle`을 `layout.ts`로 옮겨 렌더러와 캐럿이 **같은 답**을 읽게 했습니다. 답이 둘이면 캐럿이 글자 옆이 아니라 글자 위에 섭니다.

Tab이 열을 따라 다음 뉴런으로 넘어갑니다. 층에 라벨 다는 게 이걸 쓰는 유일한 이유인데, 원마다 마우스로 더블클릭하면 그건 노동입니다.

### MathJax는 에러를 `merror`로 안 준다

타이핑 도중은 **계속 틀린 TeX**입니다 — `\sigma`로 가는 길에 `\sig`를 반드시 거칩니다. 그때 원 안에 빨간 에러 글자가 번쩍이면 못 씁니다.

`parseMathJaxSvg`의 `error` 판정이 `/data-mjx-error|merror/`였는데, **SVG 출력은 둘 다 안 냅니다.** 실제로는 `<g data-mml-node="mtext" fill="red" stroke="red">`입니다. 즉 이 플래그는 처음부터 한 번도 true가 된 적이 없습니다.

마크업을 훑는 대신 TeX 단계에서 잡았습니다. `AllPackages`에 들어 있는 **`noundefined`와 `noerrors`를 빼고** `formatError`가 throw하게 하면, 잘못된 입력에서 `convert`가 예외를 던지고 `measureMath`가 잡아 `error: true`를 줍니다. 그러면 `layoutLabel`이 그 수식 자리에 **원문을 그대로** 씁니다.

이건 원 안뿐 아니라 모든 라벨에 적용됩니다. 빨간 에러 글자보다 원문이 낫습니다 — 고쳐야 할 게 뭔지 보이고, 레이아웃을 망가뜨리지 않고, 실수로 export되지도 않습니다.

### 뉴런 포커스는 노드 포커스와 다른 대상이다

원래는 부분 패널이 노드 패널 **위에** 얹혀 있었습니다. 그러면 스와치를 눌렀을 때 노드 패널 쪽 색 버튼이 네트워크 전체를 칠합니다 — 손으로 색칠해 둔 뉴런이 여럿이면 한 번의 클릭으로 다 날아갑니다.

⚠️ 이제 부분이 포커스되면 **노드 패널을 렌더하지 않습니다.** 화면의 모든 컨트롤이 그 뉴런의 것입니다. 대신 사라진 게 버그로 읽히지 않도록 "네트워크 전체로 (Esc)" 버튼을 패널 맨 위에 뒀습니다.

**그런데 패널만 막으면 절반입니다** — 툴바에도 색을 칠하는 길이 따로 있습니다. `applyAccent`와 무채색 스와치는 포커스된 부분을 보지 않고 곧장 `patchNodeStyle`로 가서, 뉴런 하나를 골라 둔 상태에서 위쪽 스와치를 누르면 **모든 뉴런이 함께 바뀌었습니다.** 인스펙터를 고치면서 이쪽을 빠뜨린 게 원인입니다. `focusedPart()`를 한 군데 두고 색을 칠하는 모든 경로가 먼저 물어보게 했습니다.

⚠️ **Delete도 같은 함정이고 결말이 더 나쁩니다.** 뉴런은 지울 수 있는 대상이 아니라(개수는 격자가 정합니다) 그대로 흘러내려가 **네트워크를 통째로 지웠습니다.** 부분이 잃을 수 있는 건 그 부분에 해 둔 것뿐이라, 이제 오버라이드만 지웁니다.

`patchMlpPart`를 `doc.ts`로 빼서 인스펙터와 원 안 타이핑이 같은 연산을 씁니다. 비워진 bag은 **삭제**합니다 — 부분을 건드린 적 없는 `mlp`는 이 기능이 생기기 전과 똑같이 직렬화되어야 합니다. 그리고 "기본값으로"는 현재 색을 써넣는 게 아니라 항목을 지웁니다. 써넣으면 팔레트를 바꿀 때 그 뉴런만 옛 색에 남습니다.

### 반지름 기본값 16

⚠️ `MLP_R`·`MLP_PITCH`·`MLP_GAP`은 서로 묶여 있습니다. `mlpSnapProps`가 간격을 `2r` 밑으로 못 내려가게 막으므로, 반지름만 올리면 간격은 clamp되고 상수는 실제 결과를 설명하지 못하게 됩니다. r 16이면 지름 32라 pitch를 40으로, gap을 72로 같이 올렸고, 카탈로그의 박스도 176×192로 맞췄습니다(격자 모드에서 박스는 격자에서 재계산되므로 어긋나면 처음 만질 때 도형이 튑니다).

## 2026-08-13 — AI Figure Maker: MLP를 주소 가능한 격자로 다시 짜고, LaTeX를 글꼴 선택지로 올림

- 변경 파일: `src/tools/aifig/mlp.ts`(신규), `mlp.test.ts`(신규), `MathInput.tsx`(신규), `latex.test.ts`(신규), `types.ts`, `latex.ts`, `layout.ts`, `presets.ts`, `shapes.tsx`, `resolve.ts`, `doc.ts`, `geometry.ts`, `Inspector.tsx`, `Overlay.tsx`, `AiFigureMaker.tsx`, `AiFigureMaker.css`
- 요약: MLP 글리프 내부에 뉴런·시냅스라는 주소를 만들어 색·텍스트·연결·라벨을 개별로 다룰 수 있게 하고, 배치를 격자에 강제했습니다. 별도로 라벨의 텍스트 모드에 LaTeX를 추가했습니다.

### 4번 요구사항이 "MLP를 노드 그룹으로 풀까?"의 답이었다

요구는 아홉 개였는데(등간격 · ⋮ 생략 · 간선 색 · 스냅 강제 · 원 안 텍스트 · 층 라벨 · 뉴런별 연결 · 뉴런별 색 · "차라리 그룹으로?"), 마지막 항목이 나머지의 전제였습니다. 3·5·7·8은 전부 "내부의 이것"을 지목해야 하는데, 당시 `Mlp`는 `<line>`과 `<circle>`을 뱉는 `<g>` 하나였고 히트 영역은 노드당 사각형 하나뿐이라 **지목할 대상이 존재하지 않았습니다.**

처음엔 뉴런을 진짜 `ellipse` 노드로 푸는 쪽(요구 9의 문면)을 권했습니다. 그런데 **4번이 "원들 크기나 위치도 전부 스냅되도록 알고리즘 상에서 강제되어야함"**이라, 자유롭게 끌 수 있는 독립 노드는 애초에 답이 될 수 없습니다. 격자를 소유하는 주체가 있어야 하고, 그러면 뉴런은 노드가 아니어야 합니다. 게다가 노드로 풀면 "층"이라는 개념이 사라져 2번(생략)과 6번(층 라벨)이 **오히려 막힙니다** — 무엇을 생략하고 어디에 라벨을 붙일지 아는 주체가 없어지니까요. 16-16-16이면 간선만 512개가 되는 건 그 다음 문제입니다.

⚠️ **그래서 노드는 하나로 두고 내부에 주소를 냈습니다.** 되돌리려면 `EndPoint.part`와 `props.neurons`/`props.wires`를 걷어내야 하는데, 그 시점엔 이미 그걸로 만든 그림들이 있을 겁니다.

### 격자는 원을 하나씩 반올림해서 맞추면 안 된다

원마다 `snapPos`를 돌리면 격자에는 앉지만 **1번(등간격)이 깨집니다.** 간격이 원마다 미세하게 달라지니까요. 두 요구가 정면으로 부딪힙니다.

그래서 좌표를 굴리는 대신 매개변수에 조건을 걸었습니다. 모든 좌표를 노드 중심에서 재면 슬롯 `s`개짜리 열은 `cy + (k - (s-1)/2)·pitch`에 놓이고, 오프셋은 `s`가 홀수면 `pitch`의 정수배, 짝수면 `pitch/2`의 정수배입니다. 에디터의 위치 격자는 반칸(`grid/2`)이므로

    r        grid/2 의 배수
    pitch    grid 의 배수
    layerGap grid 의 배수

이면 **모든 열의 모든 원이 한꺼번에** 반칸 격자에 앉습니다. 반올림 없이, 등간격을 유지한 채로. `mlpSnapProps`가 이 세 값만 정리하고, `mlp.test.ts`가 네 가지 형태(홀수·짝수 열, 생략 있는 열 포함)로 증명을 박아 뒀습니다.

여기서 나오는 따름정리 하나: **격자 모드에서 상자는 입력이 아니라 결과입니다.** 그래서 `refitMlp`가 매 편집마다 `w/h`를 격자에서 다시 계산하고(`commit`/`live` 양쪽이 지나는 `normalise`), 리사이즈 핸들은 상자를 늘리는 대신 `pitch`/`layerGap`을 역산합니다. 이걸 안 하면 핸들이 죽은 것처럼 보입니다 — 끌어도 `refitMlp`가 즉시 되돌리니까요. 핸들 반대편 모서리를 고정하는 계산도 그 자리에 같이 있습니다.

`pitch`가 없는 노드는 **옛 stretch 동작 그대로**입니다. 기존 파일과 템플릿 4개가 그 모양으로 저장돼 있어서, 켜는 건 "등간격" 체크박스입니다.

### 부분 키는 슬롯이 아니라 유닛으로

`l0n2`, `l0n2-n1`은 **논리 인덱스**입니다. 그려진 슬롯 번호로 키를 잡으면 생략 임계값(`maxDots`)을 만지는 순간 색칠해 둔 뉴런과 붙여 둔 연결선이 죄다 다른 자리로 옮겨갑니다. 40개짜리 층에서 마지막 유닛은 슬롯 5에 그려지지만 키는 `l0n39`고, 임계값을 8에서 4로 바꿔도 그대로입니다.

⚠️ **부분은 사라질 수 있습니다** — 층을 줄이거나 생략 임계값을 낮추면 그 유닛은 안 그려집니다. 이때 `resolve.ts`는 연결선을 버리지 않고 **노드 전체로 폴백**합니다. 층 크기를 바꾼 편집이 조용히 간선을 지우면 안 되니까요. `mlpAnchorPoint`가 `null`을 주는 게 그 신호입니다.

### 히트는 DOM이 아니라 기하로

도형에 `data-mlp-dot`/`data-mlp-wire`를 붙여 두긴 했지만 **집는 데는 안 씁니다.** 에디터의 히트 레이어는 노드당 투명 사각형이라 클릭이 항상 그 사각형에 도착하고, `elementFromPoint`로 캐도 맨 위에 있는 것만 잡힙니다. `mlpDotAt`/`mlpWireAt`이 그리기와 같은 좌표로 직접 판정합니다. 원이 선을 이깁니다 — 시냅스는 뉴런 밑에서 끝나고, 교차점에서 겨냥한 건 뉴런이니까요.

선택은 모드를 안 만들었습니다: **먼저 노드를 고르고, 고른 상태에서 내부를 누르면** 그 부분이 잡힙니다(일러스트레이터의 그룹 진입과 같은 관용구). Esc는 부분 → 노드 순으로 빠집니다. `selPart`는 지우는 자리가 너무 많아서 매번 초기화하는 대신 **렌더에서 유효성을 파생**시킵니다(`activePart`).

### LaTeX는 글꼴 선택지가 됐다

`FontKind`에 `latex`를 넣었습니다. sans/serif/mono는 산문이고 수식은 `$…$`로 울타리를 치지만, latex는 **문자열 전체가 하나의 수식**이라 `\sigma`를 그냥 씁니다. 새로 그리는 노드와 연결선의 기본값이 latex입니다(템플릿은 `baseStyle`/`baseEdgeStyle`을 직접 쓰므로 영향 없음 — 그쪽 라벨은 `$…$`로 쓰여 있습니다).

⚠️ **모드를 바꾸면 원문을 다시 써야 합니다.** 안 그러면 드롭다운 한 번에 `$x^2$`가 TeX 에러가 되고 `Layer`가 이탤릭 변수 다섯 개의 곱이 됩니다. `retypeLabel`이 산문은 `\text{}`로 싸고 울타리 친 수식은 벗깁니다. 도형 카탈로그의 시드 라벨(`Layer`, `$\mathcal{L}_{\mathrm{data}}$`)도 `makeNode`에서 같은 함수를 지나갑니다. **바이트가 아니라 보이는 것이 왕복합니다** — `\text{a}`는 `$\text{a}$`로 돌아옵니다. 바이트를 맞추려면 어느 `\text{}`를 사람이 썼는지 추측해야 하고, 틀리면 사용자 마크업이 조용히 지워집니다.

층 라벨만 예외로 **항상 산문**입니다. 노드가 latex 모드일 때 `no bias`를 수식으로 읽으면 공백이 없는 수식 문법 탓에 `nobias`가 됩니다 — 요청에 나온 바로 그 예시라, 층 라벨은 주석이지 기호가 아니라고 보고 달러 규약을 유지했습니다.

편집은 `math-field`(MathLive)로 띄웁니다. ⚠️ **DOM에 붙이기 전에 `menuItems`를 세팅하면 "Mathfield not mounted"로 컴포넌트가 통째로 죽습니다** — `appendChild`가 먼저입니다. 그리고 MathLive는 800 kB라 `React.lazy`로 미룹니다(latex.ts가 MathJax에 하는 것과 같은 거래). 정적 import로 두면 상자만 옮기러 들어온 사람 앞에도 800 kB가 깔립니다.

### 함정 하나: `tsc --noEmit`은 이 저장소에서 아무것도 검사하지 않는다

루트 `tsconfig.json`은 `files: []` + references라 `npx tsc --noEmit`이 **조용히 통과합니다.** 한동안 그걸 믿고 진행했고, 실제로는 미import 타입이 있었습니다. `npx tsc -b`(또는 `npm run build`)를 써야 합니다.

브라우저 확인: 등간격 3열이 pitch 40 공유 · 32유닛 층이 4원+⋮+마지막 · `l0n0→l2n1` skip 연결이 두 원의 테두리에 정확히 붙음 · 뉴런 클릭 시 "2층 2번" 패널 · 교차 없는 지점의 시냅스 클릭이 정확히 `l0n0-n3` · e 핸들 드래그가 `layerGap` 96→120(8의 배수)로 바뀌고 좌변 x=60 고정.

## 2026-08-13 — AI Figure Maker: 연결점을 상자 밖으로 띄워 리사이즈 핸들을 되찾음

- 변경 파일: `src/tools/aifig/handles.ts`, `src/tools/aifig/Overlay.tsx`, `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/handles.test.ts`(신규)
- 요약: 연결 앵커가 변 중앙 리사이즈 핸들 네 개를 완전히 덮고 있었습니다. 앵커를 상자 밖 12px로 띄웠습니다.

### 한 점이 두 뜻을 가지면 z-order로는 못 가른다

`ANCHOR_UV`의 n·s·e·w가 `HANDLE_UV`의 같은 좌표였고, 앵커(r 5.5 원)가 핸들(8px 사각) 뒤에 그려졌습니다. 원이 사각을 완전히 덮으니 **도형에 마우스를 올린 상태에서는 변 중앙 핸들 네 개를 아예 잡을 수 없었습니다** — 누르면 연결선 드래그가 시작됐죠. 모서리 핸들만 살아 있었습니다.

순서를 바꾸는 걸로는 못 풉니다. 같은 점이니까요. 그래서 공간을 갈랐습니다: 연결점은 상자 밖 12px로 띄우고, 핸들은 테두리에 그대로 둡니다. draw.io 계열이 쓰는 방식입니다.

⚠️ **거리 세 개가 서로 물려 있습니다.** 핸들 반폭 4, 앵커 12±5.5, 회전 손잡이 28±4.5 — 앵커가 핸들에서 2.5px, 회전에서 6px 떨어집니다. 회전 손잡이를 22에서 28로 민 게 이것 때문입니다(22면 앵커와 정확히 맞닿습니다). `handles.test.ts`가 이 간격들을 박아 뒀으니 숫자를 만질 땐 테스트가 잡아 줍니다.

### hover를 넓히는 대신 붙잡았다

점이 밖으로 나가면 그걸 향해 마우스를 뻗는 순간 도형을 벗어나고, `pointOnNode`의 9px 여유가 끝나는 지점에서 hover가 풀려 **잡으려던 점이 사라집니다.** 이미 같은 함정이 한 번 있었고(예전 pointerenter/leave 시절) 그 주석이 코드에 남아 있습니다.

여유값을 17px로 키우면 풀리긴 합니다. 안 했습니다 — 도형의 halo는 **찾기** 위한 것이고, 이걸 키우면 모든 도형이 그만큼 욕심쟁이가 되어 어느 도형이 hover될지가 더 자주 어긋납니다(누르는 판정은 tol 0이라 클릭과 표시가 따로 놉니다 — 코드에 ⚠️로 경고된 그 부류의 버그). 대신 **이미 잡고 있는 노드는 포인터가 그 노드의 점 위에 있는 동안 놓지 않게** 했습니다. 찾는 반경은 9px 그대로입니다.

`hoverRef`를 따로 두고 동기적으로 씁니다. 리렌더 전에 pointermove가 여러 번 들어오는데, state를 되읽으면 값이 밀려서 정작 건너가는 중인 노드를 놓칩니다.

경계가 끊기면 안 됩니다: 점은 6.5~17.5px, halo는 0~9px — 6.5~9에서 겹치니 이동 경로에 死角이 없습니다. 이것도 테스트로 박았습니다.

### 그린 점을 옮긴 것뿐, 선이 붙는 자리는 그대로

`anchorHandlePoint`는 **Overlay가 점을 그릴 때만** 씁니다. 연결선이 실제로 어디에 붙는지는 `resolve.ts`가 앵커 이름(n/s/e/w)으로 따로 계산합니다. 그래서 점을 띄워도 그려지는 선은 1px도 안 움직입니다 — 고치기 전에 grep으로 확인했고, `resolve.ts`는 `handles`를 import하지도 않습니다.

브라우저에서 확인: e 핸들을 끌면 폭이 106→144로 늘고 새 연결선은 0개, 띄운 e 점을 끌면 연결선이 정확히 하나 생기며 `from.anchor === 'e'`. 도형 안 20px에서 밖 17px까지 훑는 동안 점의 주인이 한 번도 안 바뀌었습니다(붙잡기가 없었다면 9px에서 끊겼을 자리).

## 2026-08-13 — AI Figure Maker: 페이지도 정렬 대상으로

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/snap.test.ts`
- 요약: ⌥를 누른 채 빈 캔버스를 가리키면 네 여백을 보여주고, 드래그·리사이즈는 페이지의 가장자리와 중심에 붙습니다.

### 규칙은 하나: 가리킨 것이 답한다

기존 `probe`는 `hoverNode`가 없으면 `null`이었습니다 — 도형 위가 아니면 아무것도 안 나왔죠. 그 자리를 캔버스가 받게 했습니다. 다른 도형을 가리키면 그 도형이, 아무것도 안 가리키면 페이지가 답합니다. 선택된 도형 자신을 가리켰을 때도 페이지로 넘어갑니다 — "내가 고른 것과 나 사이의 거리"는 질문이 아니니까요. Figma의 ⌥가 부모 프레임까지의 거리를 주는 것과 같은 문법입니다.

`measureToFrame`은 여백이 0인 축을 빼먹습니다. 일부러입니다 — 길이 0인 막대는 그릴 게 없고, 그 사실은 정렬 안내선이 이미 말해 줍니다. `measureBetween`이 두 도형이 겹치는 축에 대해 침묵하는 것과 같은 규칙이라 오버레이의 어휘가 한 벌로 유지됩니다.

여백 라벨에 부호를 붙였습니다. 페이지 밖으로 삐져나간 도형을 절댓값으로 찍으면 `12`와 `-12`가 똑같이 보이는데, 그 둘을 구별하는 게 이 표시의 존재 이유입니다. 막대도 실제 위치대로 프레임 바깥에 그립니다.

### ⌥ 하나만으로는 부족했다

`measuring`은 창 전역 keydown으로 켜집니다. 그래서 인스펙터 입력칸에서 ⌥를 건드려도 캔버스에 분홍 막대가 쏟아졌습니다. `overCanvas`를 SVG의 pointerenter/leave로 따로 들고, 포인터가 스테이지 위에 있을 때만 답하게 했습니다. 이벤트 두 개뿐이라 비용도 없습니다.

### 스냅도 켰다 — 충돌은 기존 우선순위가 이미 풀어 놨다

캔버스를 `snapGuides`와 `snapSides`에 rect 하나로 더 넘깁니다. 그게 전부입니다.

격자와의 충돌은 새 규칙 없이 풀렸습니다. 드래그 경로는 이미 **안내선 → 균등 간격 → 격자** 순이고, 그 이유가 주석에 적혀 있습니다 — "눈에 보이는 것과의 정렬이 보이지 않는 격자와의 정렬을 이긴다". 페이지는 테두리까지 그려져 있는, 눈에 보이는 것입니다. 그러니 1순위에 넣으면 끝입니다.

⚠️ **이 순서가 캔버스 스냅이 성립하는 조건 자체입니다.** 격자가 양자화하는 건 페이지의 중심이 아니라 **도형의 위치**이고, 가운데 정렬은 그 위치를 `페이지/2 − 폭/2`에 놓습니다 — 528px 페이지 위의 106폭 도형이면 211, 8px 격자의 반 칸 격자(4의 배수)가 아닙니다. 격자가 먼저 돌거나 나중에라도 한마디 거들면 도형은 페이지 중앙에 **영원히 못 앉습니다.** 1순위가 축을 통째로 가져가고 `snapPos`가 그 축을 건너뛰기 때문에 되는 겁니다.

브라우저에서 실제로 끌어 확인했습니다. 이동은 460px 페이지에서 x=182(=230−96/2)로 붙었고 — 격자였다면 180 — ⌘를 누른 채로는 209에서 멈춰 211로 안 갔습니다. 리사이즈는 오른쪽 끝이 페이지 중심 264에 붙어 w=257이 됐고, 격자였다면 256입니다.

**균등 간격에는 안 넘깁니다.** 페이지는 정렬 대상이지 *도형*이 아닙니다 — 줄지어 놓은 블록들의 리듬에 페이지가 낄 자리는 없습니다. 이웃 하나 + 페이지를 "이웃 둘"로 세면 안 된다는 걸 테스트로 박아 뒀습니다.

⌘(free) 우회는 그대로입니다 — 확인했습니다.

### 곁다리로 발견한 것 (안 고침)

연결 앵커(초록 점)가 **변 중앙 리사이즈 핸들 네 개를 정확히 덮습니다.** `ANCHOR_UV`의 n·s·e·w가 `HANDLE_UV`의 같은 지점이고 앵커가 나중에 그려집니다. 그래서 도형에 올린 채로는 e/w/n/s 핸들을 잡을 수 없고, 잡으려고 누르면 연결선 드래그가 시작됩니다. 모서리 핸들은 멀쩡합니다. 이번 작업 범위가 아니라 두었습니다.

## 2026-08-12 — HWP Math: 짝 없는 LEFT·RIGHT를 나무라지 말고 채워 넣기

- 변경 파일: `src/tools/hwp.ts`, `src/tools/hwp.test.ts`
- 요약: 짝이 없으면 보이지 않는 짝(`\left.`·`\right.`)을 **묶음마다** 채웁니다.

### ⚠️ 틀린 전제를 먼저 세웠다

`LEFT` 6개 `RIGHT` 7개짜리 스크립트를 받고 "한글이 거부한다"고 진단했는데, **틀렸습니다.** 한글은 짝을 강제하지 않습니다 — 사용자가 처음에 보여준 HWP 출력물부터가 `LEFT .` 하나로 시작해 끝까지 닫지 않는 문자열이었습니다(LEFT 4, RIGHT 3). 증거가 처음부터 손에 있었는데 그걸 "HWP 내보내기의 별난 점"으로 흘려 넘기고, 정작 그 위에 짝 강제 경고를 얹었습니다.

**짝이 필요한 쪽은 LaTeX입니다.** 그것도 중괄호 묶음 안에서까지 맞아야 합니다. 그러니 원본을 나무랄 게 아니라 결과물을 컴파일되게 만들어 주는 게 맞습니다.

### 층마다 따로 채운다

`parseSeq`가 들어올 때 프레임을 쌓고 나갈 때 셉니다. 모자란 쪽에 `\left.`를 앞에, `\right.`를 뒤에 붙입니다 — 둘 다 보이지 않는 구분자라 모양은 그대로고 컴파일은 됩니다.

⚠️ **전역으로 세면 안 됩니다.** `{ LEFT ( x } + RIGHT ) y`처럼 묶음 경계를 넘나들면, 전체 개수는 1:1이라 아무 문제 없어 보이지만 LaTeX는 각 중괄호 안에서 짝을 요구해 그대로 깨집니다. 층마다 세면 `\left.{\left( x\right.} + \right) y`가 되어 통과합니다. KaTeX에 실제로 넣어 네 경우 다 파싱되는 걸 확인했습니다.

정방향의 경고도 함께 고쳤습니다. 짝 없는 `\left`는 **원본 LaTeX**의 문제이지 한글 쪽 문제가 아니므로 그렇게 적습니다. 그리고 `\{`는 짝이 필요 없는 그냥 글자라 세지 않습니다.

## 2026-08-12 — HWP Math: 기호 커버리지를 실제로 세어서 메움

- 변경 파일: `src/tools/hwp.ts`, `src/tools/hwp.test.ts`
- 요약: LaTeX→한글 61% → **95%**, 한글→LaTeX 88% → **100%**.

### 짐작하지 말고 세기

"웬만한 건 다 들어가면 좋겠다"는 말에 표를 눈으로 훑는 대신 임시 테스트를 하나 써서 셌습니다.

- **LaTeX 쪽 기준**: `node_modules/katex/src/symbols.ts`에서 명령어 496개를 뽑아 각각 변환기에 넣고 "찾지 못했다" 경고가 나오는지 봅니다. KaTeX 목록을 쓴 이유는 **이 도구가 미리보기를 그릴 때 쓰는 바로 그 목록**이라, 그릴 수 있는 것과 옮길 수 있는 것이 어긋나지 않기 때문입니다.
- **한글 쪽 기준**: OpenBapul 변환표의 이름 173개. 되읽었을 때 입력과 출력이 같으면(=글자로 통과했으면) 미커버.

처음 결과가 **301/496 미커버(39%)**, 20/173. 짐작보다 훨씬 나빴습니다.

### 이름이 없으면 글자로

301개를 하나씩 이름 붙일 수는 없습니다 — 한글의 기호 팔레트가 LaTeX보다 훨씬 작아서 애초에 이름이 없는 게 대부분이니까요. 답은 스펙 안에 있었습니다. 예제 2.1이 `□`와 `÷`를 두고 **"코드표를 이용해 삽입한다"** 고 적어 둡니다. 이름 없는 기호는 글자로 넣는 게 한글의 정식 방법이라는 뜻입니다.

그래서 KaTeX의 명령어→유니코드 표를 뽑아 `UNICODE` 폴백을 만들었습니다. 이름을 못 찾으면 낱말을 그대로 찍는 대신(수식 안에 `heartsuit`라는 글자가 남습니다) 그 기호를 넣습니다. 되돌릴 때는 같은 표를 뒤집어 글자를 명령어로 되읽고요. 234개가 한 번에 해결됐습니다.

⚠️ 글자가 들어간다고 반드시 보이는 건 아닙니다 — 한글 수식 글꼴에 글리프가 없으면 네모가 됩니다. 그래서 이렇게 옮긴 기호는 **한 줄로 모아** 알립니다. 하나씩 경고하면 기호 다섯 개짜리 수식이 경고 다섯 줄을 답니다.

### 뒤집을 때 먼저 적힌 쪽이 이긴다

⚠️ 손으로 더한 항목을 표 앞에 붙였더니 `⋈`가 `\bowtie`가 아니라 `\Join`으로 되돌아왔습니다(알파벳순 자동 생성분보다 먼저 놓여서). 손으로 더한 블록은 **표 끝에** 둡니다.

⚠️ 그리고 폴백을 뒤집을 때 **아스키는 제외**해야 합니다. KaTeX에 `\lparen`이라는 이름이 있다는 이유로 `(`가 `\lparen`으로 되돌아와 왕복 테스트가 깨졌습니다. `(`는 LaTeX에서도 그냥 `(`입니다.

### 남은 26개는 고칠 게 아닙니다

전부 본문 모드입니다 — 라틴 합자(`\ae \OE \ss`), 본문 악센트(`\H \c \v`), 절 기호(`\P \S`), 그리고 한글에서 **뜻이 따로 있는 글자들**(`\textunderscore`·`\textasciicircum`·`\textasciitilde`·`\textbraceleft`). 마지막 넷은 `_`·`^`·`~`·`{`가 한글에서 첨자·빈칸·묶음이라 글자로 쓸 방법이 아예 없습니다. 여기서는 경고가 정답입니다.

## 2026-08-12 — HWP Math: 한글 수식 미리보기, 그리고 선행 사례와의 대조

- 변경 파일: `src/tools/HwpMath.tsx`, `src/tools/HwpMath.css`, `src/tools/hwp.ts`, `src/tools/hwp.test.ts`, `README.md`
- 요약: 두 칸이 각자 자기 내용을 그립니다. 그 김에 기존 오픈소스 변환표와 대조해 버그 셋을 잡았습니다.

### 한글 수식을 그리는 법

따로 렌더러를 만들 필요가 없었습니다. 역방향 파서가 이미 있으니 `hwpToLatex`로 한 번 되읽어 KaTeX에 넘기면 됩니다. 그래서 LaTeX → 한글 방향에서는 미리보기가 둘 나란히 서고, **둘이 다르면 옮기는 중에 무언가 샜다는 뜻**입니다. 경고문으로 읽는 것보다 눈으로 보는 게 빠릅니다.

⚠️ 다만 그것은 **이 도구가 읽은** 스크립트지 한글이 읽은 스크립트가 아닙니다. 두 방향이 같은 맹점을 공유하면 미리보기끼리는 맞아떨어지면서 정작 한글에서만 다르게 보일 수 있습니다. 미리보기는 확인이지 보증이 아닙니다.

### 선행 사례 대조

[OpenBapul/hml-equation-parser](https://github.com/OpenBapul/hml-equation-parser)(Apache-2.0, Python)는 같은 역방향을 하는 오픈소스입니다. 실제 HML 문서에서 뽑아 만든 변환표라 스펙보다 넓습니다. 표를 받아 대조하니 `NABLA`·`Partial`·`LAPLACE`·그리스 대문자·`union`/`inter`·`larrow`/`LARROW` 대소문자 규칙까지 독립적으로 일치했습니다. 그리고 셋이 어긋났습니다.

1. **`XOR` → `\xor`** — LaTeX에 없는 명령입니다. 정방향 표를 뒤집어 역방향을 만드는 구조라, 표에 적힌 이름이 곧 출력이 됩니다. `veebar`를 먼저 적어 `\veebar`가 이기게 했습니다.
2. **`IDENTICAL`을 `\equiv`로** 두었는데 그건 ≡가 아니라 비례식의 ∷입니다. 조용히 다른 뜻이 되던 자리라, 대응 명령이 없는 대로 글자를 그대로 씁니다.
3. **꺾쇠괄호를 `<`로** 내보내고 경고했는데, 한글은 `LEFT ⟨`처럼 유니코드 글자를 직접 씁니다(그 저장소의 README 예제가 `LEFT ⌊`입니다). 경고를 지우고 제 글자로 내보냅니다. ⚠️ 되돌릴 때 `<`를 `\langle`로 읽던 것도 함께 고쳤습니다 — 그러면 한글에서 나오지도 않는 괄호가 미리보기에만 그려집니다. **이건 두 미리보기를 나란히 놓자마자 눈에 띈 버그입니다.**

`BOX`·`OVERBRACE`·`UNDERBRACE`·`dyad`도 그 표에서 확인해 "대응 없음" 경고 셋을 지웠습니다. 한컴 스펙이 스스로 "주요한" 명령어만 다룬다고 밝히고 있으니, 실물에서 확인된 이름은 받아들이는 게 맞습니다.

## 2026-08-12 — HWP Math: 한글 수식 → LaTeX (역방향)

- 변경 파일: `src/tools/hwp.ts`, `src/tools/hwp.test.ts`, `src/tools/HwpMath.tsx`, `src/tools/HwpMath.css`, `public/llms.txt`, `README.md`
- 요약: 같은 도구가 양방향이 됐습니다. 툴바의 토글로 방향을 바꿉니다.

### over가 정말로 몇 개를 가져가는가

되돌리기의 전부가 이 질문에 달려 있었습니다. eqn 계열의 고전적인 규칙은 "앞의 것을 묶음 끝까지 전부"인데, 한컴 스펙의 예제가 그걸 반박합니다 — `10a^3 over b^2 times □`가 (10a³/b²)×□로 나옵니다. 분모가 `times`까지 삼키지 않아요.

답은 **빈칸이 항을 나눈다**는 한글의 기본 규칙 그 자체였습니다. `over`는 앞뒤에서 **정확히 한 항씩** 가져갑니다. 그래서 여러 항을 쓰려면 중괄호로 묶어야 하고(스펙이 계속 강조하던 게 이것), 되돌릴 때도 앞뒤 한 항씩만 집으면 됩니다. 예제 넷을 다 이 규칙으로 확인했습니다.

⚠️ 다만 "한 항"에는 **거기 달라붙은 첨자까지** 포함됩니다. `b^2`의 `b`만 집으면 제곱이 분수 바깥으로 새어 나가 `\frac{a^3}{b}^2`가 됩니다. `parseOperand`가 항 하나와 뒤따르는 `_`/`^`를 함께 먹습니다.

### 되돌리기는 대칭이 아니다

세 가지가 다릅니다.

**모르는 낱말에 경고하지 않습니다.** 정방향에서는 대응 없는 명령어가 진짜 문제였지만, 역방향에서 `abc`는 그냥 기울임체 글자 셋입니다 — 빠진 명령어가 아니라 사용자의 변수 이름이에요. 그래서 모르는 낱말은 조용히 통과시키고, LaTeX에 정말 대응이 없는 것들(LADDER·LONGDIV·REL·SCALE처럼 이름을 아는 것들)만 골라 경고합니다.

**대소문자 규칙이 여기서 처음 문제가 됩니다.** 한글은 명령어 대소문자를 대체로 안 가리는데 `larrow`(←)와 `LARROW`(⇐), `vert`(|)와 `VERT`(‖), `deg`와 `DEG`(°), 그리고 그리스 문자는 가립니다. 규칙은 "모두 소문자면 작은 쪽, 대문자가 하나라도 섞이면 큰 쪽" — 한글이 스스로 `NABLA`와 `Partial`을 섞어 내보내는 것과 같은 규칙입니다. `CASED` 표가 그것만 따로 처리하고 나머지는 소문자 키로 찾습니다.

**LaTeX는 수식 안의 빈칸을 무시합니다.** 그래서 이쪽에서 띄어쓰기는 오로지 읽기용입니다. 그래도 한글 원본의 띄어쓰기를 그대로 살립니다 — 한글에서도 그 빈칸은 뜻이 없었지만, 그렇게 나눠 적은 사람이 그 식을 그렇게 읽고 있으니까요.

⚠️ 그 때문에 생긴 함정: 첨자나 근호를 찾아 **앞을 내다볼 때 빈칸을 먹어 치우면 안 됩니다**. 그 빈칸이 다음 항이 원본에서 떨어져 있었다는 유일한 증거라, `\right)^{n}= e`처럼 등호가 앞에 붙어 버립니다. 내다보기 전에 위치를 저장하고 못 찾으면 되돌립니다.

### 표는 하나만 유지합니다

기호표를 두 벌 두면 반드시 어긋납니다. `REV`는 정방향 표(`SAME`·`GREEK_LOWER`·`FUNCS`·`RENAME`·`ACCENT`)를 뒤집어 만들고, 이름이 여럿인 것(`\le`/`\leq`)만 `REV_FIX`로 덮어씁니다. 새 기호를 앞에 추가하면 양방향이 함께 따라옵니다.

### 왕복 테스트

글자까지 같아지길 기대할 수는 없습니다 — 빈칸과 중괄호의 쓰임이 양쪽에서 다르니까요. 그래서 왕복 테스트는 빈칸과 중괄호를 지운 뒤 비교합니다. 다섯 식이 한 바퀴 돌아 제자리로 옵니다. 테스트 61개.

### 화면

색을 **칸의 자리가 아니라 내용**에 묶었습니다. 보라는 언제나 LaTeX, 분홍은 언제나 한글입니다. 자리를 따라가게 두면 토글할 때마다 두 칸이 색을 맞바꿔서, 무엇을 보고 있는지 매번 다시 읽어야 합니다. 미리보기도 같은 이유로 LaTeX 쪽 칸을 따라 옮겨 다닙니다.

원본은 방향마다 따로 기억합니다. 토글이 상대편 칸을 덮어쓰면, 한글 문서에서 붙여 넣어 둔 것이 LaTeX를 한 번 옮겼다고 사라집니다.

## 2026-08-12 — HWP Math: LaTeX를 한글 수식 스크립트로

- 변경 파일: `src/tools/hwp.ts`, `src/tools/hwp.test.ts`, `src/tools/HwpMath.tsx`, `src/tools/HwpMath.css`, `src/project.ts`, `src/App.tsx`, `public/llms.txt`, `README.md`
- 요약: 열한 번째 도구. LaTeX를 한컴오피스 한글의 수식 편집기 문법으로 옮깁니다.

### 왜 치환표가 아니라 파서인가

처음엔 정규식 몇 줄이면 될 것 같았는데 아닙니다. 한글 수식의 `over`는 **중위 연산자**라 `\frac{a}{b}`를 옮기려면 두 인자가 어디서 끝나는지를 먼저 알아야 하고, `x^{2}`의 밑을 찾으려면 바로 앞 항이 어디서 시작하는지 알아야 합니다. 중괄호 짝을 세는 순간 이미 파서이므로, 처음부터 재귀 하강으로 썼습니다. 덕분에 `\begin{pmatrix}`의 `&`·`\\`가 `&`·`#`으로 그냥 흘러갑니다 — 환경은 본문을 파싱한 뒤 이름만 씌우면 끝입니다.

### 문법 출처

한컴이 공개한 [한글 문서 파일 형식 — 수식 revision 1.3](https://cdn.hancom.com/link/docs/한글문서파일형식_수식_revision1.3.pdf) PDF와 [한컴오피스 도움말](https://help.hancom.com/hoffice/multi/ko_kr/hwp/insert/equation/equation(explanation).htm)입니다. 스펙에 없는 이름은 **지어내지 않고 경고로 넘깁니다** — 한글은 모르는 낱말을 조용히 다른 기호로 바꾸는 게 아니라 그 글자 그대로 찍기 때문에, 사용자가 어디를 손봐야 하는지 알려주는 편이 낫습니다.

스펙에서 건진 것 중 헷갈리기 쉬운 것들:

- `UNION`/`INTER`는 첨자가 붙는 **큰** 기호이고, 줄 안에 놓을 작은 것은 `SMALLUNION`/`SMALLINTER`입니다. 그래서 `\cup`→`SMALLUNION`, `\bigcup`→`UNION`.
- 화살표는 홑이 `->`·`<-`·`<->`, 겹이 `RARROW`·`LARROW`·`LRARROW`입니다. `\Leftarrow`를 `<=`로 옮기지 않은 이유는 한글에서 `<=`가 ≤로 읽힐 여지가 있어서입니다.
- LaTeX의 `\sup`(상한)은 한글에서 **윗첨자 명령**입니다. 그대로 두면 수식이 깨져서 `{rm sup}`으로 옮깁니다.
- 그리스 대문자는 `Delta`처럼 첫 글자만 대문자. 스펙이 "첫 글자를 대문자로 적어 둔 것은 반드시 그렇게 입력"이라고 못박고 있습니다.
- `\mathcal{L}`만은 한글에 진짜 글자가 있습니다 — 라플라스 변환 기호 `LAPLACE`. 나머지 필기체는 경고 후 로만체.

### ⚠️ 함정 둘

**한글에는 "글자로서의 중괄호"가 없습니다.** 중괄호는 언제나 항의 묶음이고, 보이는 중괄호는 `LEFT {` … `RIGHT }`뿐입니다. 그래서 `\{` 하나만 있어도 LEFT/RIGHT 쌍으로 옮기고, 짝이 안 맞으면 경고합니다. 여기서 파생된 두 번째 함정 — `\left\{G\right\}^{2}`에서 첨자의 밑이 `RIGHT }`인데, 이걸 평소처럼 `{RIGHT }}`로 묶으면 닫는 중괄호가 묶음 기호로 읽혀 수식이 통째로 깨집니다. `flat` 플래그를 둬서 괄호 항과 이미 첨자가 달린 항은 묶지 않고 옆에 이어 붙입니다(`RIGHT } ^{2}`). `x_1^2`가 `{x_1}^2`가 아닌 것도 같은 플래그로 처리됩니다.

**`LEFT {G`처럼 붙여 쓰지 않습니다.** 다른 괄호는 다음 글자를 붙여도 되지만(`LEFT (X`), 중괄호만은 한글이 괄호가 아니라 묶음의 시작으로 읽을 여지가 있어 빈칸을 남깁니다. 한글이 스스로 내보내는 문자열도 `LEFT { G(X) RIGHT }`로 띄어 있습니다.

### 빈칸

한글에서 빈칸은 **항의 경계일 뿐 화면에 나오지 않습니다**. 그래서 항 사이를 아무리 띄워도 결과는 같은데, 그렇다고 다 띄우면 `(X,Y)`가 `( X , Y )`가 되어 읽을 수가 없습니다. 원본에서 붙어 있던 글자는 붙여 두고 원본에 있던 빈칸은 살리도록 항마다 `tightLeft`/`tightRight`/`gap`을 답니다. 결과가 원본과 같은 모양으로 읽혀야 사용자가 어디를 고칠지 찾을 수 있습니다.

### 화면

체스판 무대(`.stage`)를 쓰지 않았습니다 — 투명 배경을 알릴 것이 없는데 체스판을 깔면 "여기서 PNG가 나온다"는 거짓말이 됩니다. 대신 두 칸: 왼쪽에 원본과 KaTeX 미리보기, 오른쪽에 결과와 경고. 미리보기가 LaTeX 쪽인 이유는 한글이 무엇을 그릴지가 아니라 **내가 무엇을 넣었는지**를 확인하는 자리이기 때문입니다.

## 2026-08-12 — AI Figure Maker: ⌥ 계측 (피그마 방식)

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/aifig/Overlay.tsx`, `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/snap.test.ts`
- 요약: Alt를 누른 채 다른 도형에 올리면 선택한 것과의 거리·이미 맞아 있는 모서리를 띄웁니다.

거리는 **마주 보는 모서리 사이**에서만 의미가 있으므로, 어떤 축에서 두 도형이 겹치면 그 축은 아무것도 보고하지 않습니다 — 이름 붙일 간격이 없으니까요. 막대는 두 도형이 공유하는 띠를 지나가게 그리고, 공유하는 띠가 없으면 선택한 쪽에 놓고 상대 도형까지 점선으로 뻗습니다. 그래야 이 측정이 둘 사이의 직선 경로가 **아니라는** 게 보입니다.

정렬은 `snapGuides`를 임계값 0.5로 돌려 얻습니다 — 스냅이 실시간으로 그리는 것과 같은 선이라, 계측과 스냅이 같은 언어를 씁니다.

기준은 다른 정렬과 마찬가지로 **잉크**입니다. 박스는 편집용 액자일 뿐이고, 보이지 않는 모서리까지의 거리를 알고 싶은 사람은 없습니다.

⚠️ Alt는 모든 키 이벤트에 플래그로 실려 오므로 키 자체가 아니라 **플래그를 봅니다**. 창 밖에서 손을 떼면 keyup이 오지 않으므로 `blur`에서도 끕니다. (Alt+드래그는 기존대로 화면 이동입니다.)

## 2026-08-11 — AI Figure Maker: 짧은 연결선, 노드 삭제 시 그래프 잇기, 죽은 컨트롤

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/aifig/resolve.ts`, `src/tools/aifig/edges.tsx`, `src/tools/aifig/doc.ts`, `src/tools/aifig/types.ts`, `src/tools/aifig/Inspector.tsx`, `src/tools/AiFigureMaker.tsx`, 테스트 2종

### 자기 장식보다 짧은 연결선

거의 붙어 있는 두 도형 사이에, 그 틈보다 훨씬 큰 연결선이 그려졌습니다. 실측(간격 2px):

| 경로 | 증상 |
| --- | --- |
| 곡선 | 실제 길이 **31px** — 2px 틈을 31px 고리로 |
| 호 | 옆으로 **24px** 부풀음 |
| 직선·직각 | 화살촉 4.8px에 남는 선 **0.2px** |

원인이 셋 다 같습니다 — **여유 공간을 보지 않는 상수**. 곡선 핸들에는 28px 하한이 있고, 호의 bow는 px 단위이고, 화살촉은 선 두께에서 크기를 얻습니다. 각각 실제 거리로 상한을 걸었습니다: 핸들은 거리의 절반까지, bow는 현의 절반까지, 화살촉은 선 길이의 3분의 2를 둘이 나눠 갖고 그만큼 축소됩니다.

### ⚠️ 노드를 지우면 그래프가 이어져야 합니다

블록 하나를 지우면 거기 붙은 연결선이 같이 사라져서, 체인 중간을 빼면 앞뒤를 다시 이을 방법이 손으로 다시 긋는 것밖에 없었습니다. **블록을 지우는 건 "이 단계가 없어졌다"이지 "이게 닿은 게 다 없어졌다"가 아닙니다.**

들어오는 선 각각을 나가는 선 각각에 잇습니다. 지운 노드가 연달아 있으면 살아있는 노드가 나올 때까지 걸어갑니다(순환 방지 `seen` 포함). 없던 걸 만들지는 않습니다 — 들어오는 게 없거나 나가는 게 없으면 예전처럼 그냥 사라지고, 이미 있는 연결은 중복으로 만들지 않습니다. 분기도 보존됩니다: `a→b`, `b→c`, `b→d`에서 b를 지우면 `a→c`, `a→d`.

### 죽은 컨트롤

**MLP만 아이콘이 비어 있었습니다.** 아이콘 경로가 `M6 5v0` 같은 **길이 0짜리 선분** 다섯 개였습니다 — "여기 점 하나"라는 뜻인데, 아이콘은 기본 butt 캡으로 그려지므로 길이 0인 선분에는 보여줄 폭이 없습니다. 캡 설정에 기대지 않도록 실제 원과 선으로 다시 그렸습니다. 처음엔 3×2 전결합(6줄)로 그렸다가 22px에서 뭉개져서 4줄로 줄였습니다 — 나머지 아이콘이 여백 있는 선화라 밀도를 맞춰야 합니다.

**"점 표시"를 꺼도 점이 안 꺼졌습니다** — 제가 점을 체크무늬 패턴 안에 넣어버려서, 배경의 일부이지 그리드 레이어가 아니었습니다. 투명 배경에서 체크박스가 완전히 무력했습니다. 이제 어느 배경에서든 듣습니다.

`ToolMode`·`Selection` 타입은 선언만 있고 아무도 읽지 않았습니다 — 에디터는 모드를 `connecting`에, 선택을 id 배열 두 개에 들고 있습니다. 만들지 않은 설계의 흔적이라 지웠습니다. 노드 prop은 전부 렌더러나 인스펙터가 읽고 있어 죽은 게 없었습니다(`natW`/`natH`는 "원본 비율로" 버튼이 씁니다).

### 라벨 위치 UI

"위치" 한 줄에 진행률·X오프셋·Y오프셋 셋이 몰려 줄바꿈으로 깨졌습니다. 각각 한 줄씩 이름을 달아 나눴습니다.

그리고 **단위가 길이에 비례해야 합니다.** 0.01 고정이면 짧은 선에서는 0.1px, 긴 선에서는 10px이라 한쪽은 매끄럽고 한쪽은 뚝뚝 끊깁니다. 끌 때는 경로 위 1px 단위로 끊고, 입력 칸은 **px / % 전환**을 답니다 — 저장은 여전히 비율(`labelT`)이라 선 길이가 바뀌어도 자리를 지키고, 비율은 타이핑하기 나쁜 값이라 보여줄 때만 단위를 골라 씁니다. 실측: 92px짜리 선에서 `labelT 0.47` = 47% = 43px, 양방향 일치.

## 2026-08-11 — AI Figure Maker: ⚠️ 연결선 꺾임점을 도형에 묶기 (절대 → 상대)

- 변경 파일: `src/tools/aifig/types.ts`, `src/tools/aifig/resolve.ts`, `src/tools/aifig/doc.ts`, `src/tools/aifig/Overlay.tsx`, `src/tools/AiFigureMaker.tsx`
- 요약: 꺾임점이 캔버스 절대 좌표라 도형을 옮기면 선만 남아 꼬이던 것을, 끝점 노드 기준 상대 좌표로 바꿨습니다.

**선은 두 도형 사이의 관계이지 종이 위의 자국이 아닙니다.** 그런데 `waypoints`가 절대 좌표라 블록을 옮기면 꺾임점이 제자리에 못 박혀 그림이 엉켰습니다. 구간 끌기를 넣으면서 꺾임점이 훨씬 흔해졌으니 더 이상 미룰 수 없는 문제였습니다.

`Waypoint`에 `rel?: 'from' | 'to'`를 넣었습니다. 있으면 x/y는 **그 끝점 노드 중심으로부터의 오프셋**이고, 없으면 예전대로 절대 좌표입니다(예전 파일이 그대로 열립니다). 절대→상대 변환은 `resolveEdge` 한 곳에서만 일어납니다.

**⚠️ 기준을 앵커가 아니라 노드 중심으로 잡은 이유**: 앵커는 지금부터 이 좌표들로 *계산할* 대상입니다. 정의가 자기 결과에 의존할 수 없습니다.

**⚠️ 한 구간의 두 끝은 반드시 같은 노드에 묶어야 합니다.** 코너마다 가까운 쪽에 묶는 게 더 정밀해 보이지만 더 나쁩니다 — 직선 구간의 양 끝이 서로 다른 도형을 따라가므로, 한쪽만 옮기면 곧게 있어야 할 레인이 꺾입니다. 실측에서 `L388 → 380`짜리 삐죽한 턱이 생겼습니다. 그래서 구간을 끌 때는 **끌고 있는 구간의 중점** 기준으로 한 번만 판정해 모든 코너에 같은 `rel`을 씁니다.

기존 문서는 `normalizeDoc`에서 변환합니다. **지금 위치로 오프셋을 계산하므로 결과 좌표가 동일합니다** — 그림이 1px도 안 움직이고, 그때부터 도형을 따라다니기 시작할 뿐입니다. 실측으로 CPiMLP 문서의 path가 변환 전후 바이트 단위로 같았습니다.

부수적으로 손봐야 했던 곳: 복제·템플릿 삽입·"내용에 맞추기"의 평행이동은 상대 꺾임점을 **건드리면 안 됩니다**(이미 노드와 함께 움직였으므로 또 더하면 두 배로 갑니다). `contentBounds`도 상대 꺾임점은 제외합니다 — 노드 반경 안에 있어서 이미 계산됩니다. 오버레이의 노란 손잡이는 `ResolvedEdge.wps`(해석된 절대 좌표)를 읽습니다.

## 2026-08-11 — AI Figure Maker: 등간격 스냅, 직각 구간 끌기, 프레임 제목

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/aifig/resolve.ts`, `src/tools/aifig/shapes.tsx`, `src/tools/aifig/Overlay.tsx`, `src/tools/aifig/Inspector.tsx`, `src/tools/aifig/types.ts`, `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/snap.test.ts`

### 등간격 스냅

정렬 → 등간격 → 격자 순으로 적용합니다. 뭔가와 줄을 맞추는 게 간격을 맞추는 것보다 우선이고, 간격이 격자보다 우선입니다 — 격자는 결국 앞의 둘을 대신하는 대용품이니까요.

두 가지를 봅니다. 사이에 놓으면 **양쪽 간격이 같아지는 자리**, 줄 끝에 놓으면 **그 줄이 이미 가진 리듬**을 잇는 자리. **다른 축에서 겹치는 도형만** 후보입니다 — 다른 행에 있는 것과의 간격은 아무도 볼 수 없는 간격입니다.

실측: 100~160, 219~279 두 블록 옆에 세 번째를 떨어뜨리면 338에 붙습니다. 338은 반칸 격자 위치가 아니므로(336/340이 격자) 격자 스냅과 확실히 구분됩니다. 간격 바 두 개가 `160→219`, `279→338`로 그려집니다.

### 직각 연결선의 구간 끌기

긴 세로 구간의 x를 바꿀 방법이 없었습니다. 웨이포인트로 표현은 가능했지만, 보이는 선을 밀면 될 것을 꺾임점을 손으로 찍어 옮기는 건 순서가 거꾸로입니다.

구간을 잡아 끌면 **코너 목록 전체를 웨이포인트로 되돌려 씁니다.** 웨이포인트 라우터는 순서대로 dog-leg를 놓는데, 직각 경로의 코너는 이웃과 이미 좌표 하나를 공유하므로 그대로 재생하면 경로가 정확히 복원됩니다 — 그 구간만 다른 자리에 놓인 채로. 실측: `x=278 → 380`, 나머지 모양·라운딩 전부 동일.

양 끝 stub은 앵커에 묶여 있으므로 제외하고, 16px 미만 구간도 제외합니다(잡으려던 게 아닐 테니까요). 프리필렛 코너가 필요해서 `orthoCorners`를 노출하고 `ResolvedEdge.corners`로 실어 나릅니다 — 라운딩된 path에는 잡을 코너가 남아있지 않습니다.

### ⚠️ 프레임 제목 배경이 흰색으로 박혀 있었습니다

`fill="#ffffff"` 하드코딩이라 투명 캔버스에 흰 덩어리가 찍혔습니다. 게다가 제목이 위쪽 테두리에 **걸쳐** 있어서 판때기가 글자보다 커야만 했습니다(테두리를 가려야 하니까).

제목을 테두리 **위로** 올리면 판때기가 아예 필요 없습니다 — 뒤에 가릴 게 없으니까요. `props.titleBg`는 제목이 뭔가와 겹칠 때를 위해 남겨두되 기본은 꺼짐입니다. 제목이 박스 밖으로 나가므로 `shapeOverflow`가 그만큼 위로 넓어져야 선택·히트 영역이 맞습니다.

## 2026-08-11 — AI Figure Maker: 수식이 도형 중앙에 안 맞던 문제

- 변경 파일: `src/tools/aifig/latex.ts`, `src/tools/aifig/layout.ts`
- 요약: 라벨을 타이포그래피 상자가 아니라 실제 글리프 기준으로 가운데 맞춥니다. 수식 한정입니다.

원 안의 `$\frac{\partial \hat T}{\partial t}$`가 미세하게 아래로 내려가 보였습니다. 폰트 22px에서 재보니 내용마다 다릅니다 — 분수 +0.78px, `$x$` +0.98px, `$x^2$` −2.52px.

원인이 둘입니다. 하나는 **줄 상자가 항상 `ascent 0.76em / descent 0.24em`으로 패딩된다**는 것 — 수식이 뭐든 상관없이요. 그래서 상자를 가운데 맞추면 글리프는 어긋납니다. `LabelLayout.ink`를 추가해 라벨이 **수식으로만** 이루어진 경우 실제 상하한을 들고 다니고, `labelPos: 'center'`가 그걸 씁니다.

⚠️ **산문은 일부러 제외했습니다.** 크기가 같은 두 라벨은 한쪽에 디센더가 있든 없든 베이스라인이 같아야 합니다. 잉크 기준으로 맞추면 "AB"와 "Ag"가 서로 다른 높이에 앉습니다.

이것만으론 분수가 그대로 0.78px 남았습니다. 두 번째 원인은 **MathJax가 알려주는 상자가 레이아웃 상자**라는 것입니다 — 분수는 TeX가 규정하는 rule spacing을 품고 있어서 상자가 글리프보다 아래로 몇백분의 1 em 더 내려갑니다. 줄을 쌓을 땐 맞는 값이지만 원 안에 가운데 놓을 땐 틀립니다.

마크업만 보고는 알 수 없어서(모든 path를 순회해야 합니다) **숨긴 `<svg>`에 한 번 붙여서 `getBBox()`로 직접 잽니다.** 수식별로 한 번만 변환·캐시되므로 문서에 있는 서로 다른 수식 수만큼만 reflow가 일어납니다. `display: none`이 아니라 `visibility: hidden`이어야 합니다 — 표시되지 않는 요소의 `getBBox()`는 아무것도 알려주지 않습니다.

결과: 분수·`$x$`·`$x^2$` 모두 오차 0.001px. 일반 텍스트는 폰트 메트릭 편향(22px에서 −1.7px)이 그대로 남아 있습니다. 고치려면 캡 하이트 밴드 기준으로 바꿔야 하는데 기존 문서의 모든 텍스트 라벨이 0.09em씩 움직입니다.

## 2026-08-11 — AI Figure Maker: 정렬 가이드가 깜빡이던 문제

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/snap.test.ts`(신규)
- 요약: 축마다 가이드를 하나만 남기던 것을 고쳐 동시에 성립하는 정렬을 전부 그리고, 양립 불가능한 두 정렬 사이에서 승자가 뒤집히던 것을 이력으로 고정했습니다.

블록을 복제해 옆에 끌면 위와 아래가 동시에 맞는데, 가이드는 한 줄만 뜨고 그 한 줄이 위→아래→위로 초당 몇 번씩 튀었습니다. 원인이 둘입니다.

**하나. 축당 가이드가 하나였습니다.** `snapGuides`가 `bestX`/`bestY` 단일 값만 들고 있어서, 위·가운데·아래가 전부 맞아도 그릴 수 있는 건 한 줄이었습니다. 이제 고른 델타로 **실제로 맞아떨어지는 선을 전부** 내보냅니다. 좌표가 같은 것끼리는 하나로 합치고 span은 참여한 모든 사각형을 덮습니다.

**둘. ⚠️ "가장 가까운 후보 하나"로 고르면 안 됩니다.** 노드 좌표가 정수로 반올림되므로 "같은" 두 모서리도 소수점 단위로 다릅니다. 위쪽 델타 −1, 아래쪽 +1이면 포인터가 중간점을 넘나들 때마다 승자가 바뀝니다 — 이게 깜빡임의 정체입니다.

후보 델타를 **군집화해서 가장 많은 정렬이 동의하는 군집**을 고릅니다. 부수적으로 이게 더 나은 답이기도 합니다: 블록 하나가 통째로 맞는 정렬이 지나가는 이웃에 모서리 하나 걸치는 것보다 우선합니다. 실측에서 오른쪽 스택을 끌면 왼쪽 스택의 **서로 다른 세 노드**(첫 노드의 위, 가운데 노드의 중심, 끝 노드의 아래)와 동시에 맞고 세 줄이 다 뜹니다.

높이가 1~2px 다르면 위·아래는 **물리적으로 동시에 성립할 수 없습니다.** 어떤 델타로도 안 됩니다. 이 경우엔 답이 하나뿐인 게 맞고, 중요한 건 그 답이 계속 같아야 한다는 것입니다. `snapGuides`가 축별로 어디에 걸었는지(`atX`/`atY`)를 돌려주고 드래그 상태가 그걸 다음 프레임에 `sticky`로 되먹입니다. 붙어 있는 정렬은 2.5px만큼 유리하게 쳐주므로, 경쟁 후보가 확실히 더 가까워지기 전엔 자리를 내주지 않습니다.

`snap.test.ts`에 스윕 테스트를 뒀습니다 — 이력을 끄면 같은 스윕에서 답이 여러 개 나오고(옛 동작), 켜면 하나로 고정됩니다.

## 2026-08-11 — AI Figure Maker: 위치는 반칸 · 크기는 온칸, 빈 프레임이 클릭을 먹던 문제

- 변경 파일: `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/geometry.ts`, `src/tools/aifig/layout.ts`, `src/tools/aifig/edges.tsx`, `src/tools/aifig/Inspector.tsx`, `src/tools/aifig/presets.ts`
- 요약: 위치·크기의 격자 단위를 분리해 홀짝 문제를 없앴고, 채움 없는 컨테이너가 안쪽 클릭을 전부 삼키던 버그를 고쳤습니다. 연결선 라벨 끌기, 스폰 크기 정렬, 라벨 배경 기본값 없음.

### ⚠️ 채움이 없는 노드는 테두리로 잡습니다

먼저 이것부터. 그룹 프레임은 다른 도형들을 **둘러싸는 점선 테두리**인데, 히트 영역이 `fill="transparent"`인 꽉 찬 사각형이었습니다. 그래서 프레임 안쪽 노드를 누르면 **프레임이 잡혔습니다.** 실측으로 노드 A의 정중앙을 찍어도 `data-hit-node`가 `frame`으로 나왔습니다. 사용자 눈에는 "노드를 끌었는데 공간이 통째로 끌린다"로 보입니다 — 실제로는 프레임을 끈 것이고요.

`hitsByOutline(n)` = 채움이 `none`이고 kind가 text·image·curve·op가 아닌 노드. 이들은 히트 영역이 **굵은 투명 테두리**가 되고, 기하 판정(`pointOnNode`)도 바깥 상자 안이면서 안쪽 상자 밖일 때만 참입니다. text를 뺀 이유는 텍스트의 실체가 글자라서고(그래서 `inkRect`로 잡습니다), curve·op을 뺀 이유는 작아서 테두리만 노리기가 성가시기 때문입니다.

호버와 누르기가 **같은 판정을 쓰도록** 통일했습니다(`pickNodeAt`). 예전엔 호버는 기하로 "잠기지 않은 맨 위"를 고르고 누르기는 DOM이 주는 사각형을 받아서 잠긴 노드면 그냥 흘려보냈습니다. 잠긴 노드가 위에 겹쳐 있으면 앵커 점은 아래 노드를 가리키는데 누르면 마퀴가 시작되며 선택이 날아갔습니다. 이제 DOM이 못 쓸 답을 주면 호버와 같은 방식으로 다시 고릅니다.

### ⚠️ 위치와 크기의 격자 단위를 분리합니다

짝수 칸 박스는 모서리를 격자선에 놓는 것과 중심을 놓는 것이 **같은 위치**입니다. 홀수 칸은 아닙니다 — 중심을 선에 놓으면 모서리가 반칸에, 모서리를 놓으면 중심이 반칸에 갑니다. 제3의 답이 없으므로 **단일 격자로는 둘 중 한 경우를 반드시 틀립니다.**

처음엔 "모서리 후보와 중심 후보 중 가까운 쪽"이라는 패리티 규칙을 넣었는데, 사용자가 더 나은 답을 냈습니다: **크기는 온칸 단위, 위치는 반칸 단위.** 크기가 항상 정수 칸이므로 박스는 체크무늬에 딱 맞거나 반칸 밀린 두 상태만 가질 수 있고, 어느 쪽인지는 사용자가 놓는 자리로 정합니다. 홀짝 분기가 코드에서 통째로 사라집니다. 덤으로 반칸 격자는 화면이 이미 그리고 있는 것(칸 모서리 + 칸 가운데 점)과 정확히 일치합니다.

리사이즈는 **잡지 않은 쪽 모서리를 고정**하고 크기만 온칸으로 반올림합니다. 움직이는 모서리를 격자에 맞추면 반칸에 놓아둔 박스가 리사이즈할 때마다 온칸으로 끌려갑니다. 이 방식이면 오프셋이 보존되고, 정수 칸이 아닌 기존 크기는 첫 리사이즈에 저절로 교정됩니다.

`fitNodeToGrid()`(인스펙터 "격자에 맞추기")와 왼쪽 탭 스폰이 같은 함수를 씁니다. 도형 카탈로그의 크기(76×48, 26×26 …)는 보기 좋으라고 고른 값이지 8로 나누어떨어지라고 고른 값이 아니어서 새로 놓는 족족 격자 밖에 앉았습니다. op은 원이므로 한 변만 재서 정사각을 유지합니다.

### 연결선 라벨을 경로 위에서 끌기

라벨을 잡아 끌면 `labelT`만 바뀌고 `labelDx`/`labelDy`(선에서 띄운 오프셋)는 그대로입니다. 커서가 아니라 **라벨이 타고 있는 경로 위의 점**을 포인터 변화량만큼 옮긴 뒤 `nearestT()`로 되묻는 방식이라, 경로를 벗어나는 방향으로 흔들어도 위치만 미끄러집니다.

히트 영역(`edgeLabelBox`)은 렌더러와 **같은 함수**에서 나와야 합니다. 따로 계산하면 글자와 잡히는 자리가 어긋납니다. 히트 레이어에서 라벨을 **노드보다 뒤에** 그립니다 — 그림에서 라벨이 위에 있으니 히트 순서가 반대면 글자를 눌렀는데 밑의 박스가 잡힙니다.

`edgeLabelBox`와 `edgeLabelStyle`은 `edges.tsx`가 아니라 `layout.ts`에 있습니다. 컴포넌트 파일이 함수를 export하면 `react-refresh/only-export-components`에 걸립니다.

### 라벨 배경 기본값 없음

`labelBg`는 라벨이 선 위에 앉을 때 선을 뚫어주려고 있는 건데 대부분의 라벨은 그렇지 않아서, 기본값이 흰 판이면 배경만 잡아먹습니다. `baseEdgeStyle`을 `'none'`으로 바꿨습니다. 기존 문서는 저장된 값을 그대로 씁니다.

## 2026-08-11 — AI Figure Maker: 스냅을 보이는 것 기준으로, 체크무늬가 곧 그리드

- 변경 파일: `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/geometry.ts`, `src/tools/aifig/layout.ts`, `src/tools/aifig/Inspector.tsx`, `src/tools/aifig/types.ts`, `src/tools/aifig/presets.ts`, `src/tools/aifig/resolve.ts`, `src/tools/aifig/edges.tsx`
- 요약: "스냅이 자기만의 주관을 가진다"는 증상의 원인 네 가지를 각각 고치고, 배경 체크무늬를 그리드와 같은 단위로 통일했습니다.

### ⚠️ 그리드와 정렬 가이드를 겹쳐서 적용하면 안 됩니다

이동 처리가 **그리드로 반올림한 뒤 그 위에 가이드가 최대 5px를 더 당기는** 순서였습니다. 이웃의 모서리·중심이 그리드에 안 맞으면 — 폭이 8의 배수가 아닌 노드는 중심이 항상 반 칸 어긋나므로 대부분 그렇습니다 — 가이드가 결과를 그리드 밖으로 밀고, 다음 드래그는 다시 그리드로 당깁니다. 붙었다 안 붙었다 하는 체감이 전부 이 flip-flop이었습니다.

**가이드가 먼저입니다.** 축별로, 가이드가 잡은 축은 가이드가 이기고 안 잡힌 축만 그리드로 반올림합니다. 눈에 보이는 것과의 정렬이 보이지 않는 격자와의 정렬보다 우선이라는 규칙입니다.

여기서 `snapGuides`에 `hitX`/`hitY`를 추가해야 했습니다. **`dx === 0`으로는 "이미 정렬돼 있음"과 "정렬할 대상이 없음"을 구분할 수 없고**, 구분하지 못하면 이미 맞아 있는 정렬을 그리드가 도로 깨뜨립니다.

### ⚠️ 리사이즈의 그리드는 절대값이어야 합니다

이동은 "최종 좌표를 그리드에 맞춤"(절대)인데 리사이즈는 `dx = round(dx/grid)*grid`, 즉 **끈 거리를 양자화**(상대)했습니다. 원래 모서리가 어긋나 있으면 그 오프셋이 영원히 따라다녀서 아무리 리사이즈해도 격자에 못 올립니다. 끄는 모서리의 **결과 좌표**를 반올림하도록 바꿨습니다. 화살표 키도 같은 문제였고(`n.x + grid`), 이제 다음 격자선으로 이동합니다 — 방향에 따라 `floor`/`ceil`이라야 격자선을 건너뛰지 않습니다.

리사이즈에는 가이드 스냅이 아예 없었습니다. `snapSides()`를 새로 만들었습니다 — 잡고 있는 변만 움직입니다. 전체 박스를 움직이면 사용자가 고정하고 있는 반대편 변까지 끌려갑니다.

### 스냅 기준은 박스가 아니라 잉크(`inkRect`)

두 종류는 박스와 그림이 처음부터 달랐습니다. `op`은 **짧은 축에 내접하는 원**이라 정사각형이 아니면 긴 축에 죽은 공간이 생기고(선 두께 절반은 위아래로 삐져나옴), 텍스트 노드는 고정 박스 안에 글자가 떠 있을 뿐입니다. 박스로 정렬하면 보이지 않는 것을 맞추는 셈입니다.

`inkRect(n)`이 실제로 칠하는 영역을 돌려주고 정렬·가이드가 이걸 봅니다. 렌더링은 안 건드리므로 기존 문서가 바뀌지 않습니다. 핸들은 그대로 박스를 조작합니다 — 박스가 리사이즈 대상이라는 사실은 변하지 않으니까요.

애초에 어긋나지 않도록도 손봤습니다. op 리사이즈는 정사각 고정입니다. 텍스트에는 `props.autoFit`을 넣어 상자가 글자를 따라가게 했습니다 — **prop이 없는 노드(예전 파일)는 자동으로 꺼짐이라 레이아웃이 얼어붙습니다.** 새로 만드는 텍스트만 켜집니다. 핸들을 잡는 순간 꺼지고(사용자가 박스를 넘겨받은 것), `autoFit`일 때는 `placeLabel`의 wrap 폭이 `Infinity`여야 합니다 — 글자에서 폭을 얻는데 그 폭으로 줄바꿈하면 상자가 매 패스마다 좁아지는 순환이 생깁니다.

재측정은 `commit`/`live` 한 곳에서 돕니다. 폰트 변경처럼 간접적으로 크기를 바꾸는 편집까지 자동으로 걸리고, `refitText`가 변화 없으면 **같은 객체를 그대로 돌려주므로** commit의 identity 검사와 드래그 프레임 렌더가 그대로 싸게 유지됩니다. undo/redo는 일부러 건너뜁니다 — 복원이지 편집이 아닙니다. MathJax가 늦게 로드되면 평문 폴백으로 잰 상자가 틀리므로 ready 콜백에서 한 번 더 돌립니다.

### 체크무늬가 곧 그리드

투명 배경 체크무늬는 **7px 정사각형 하드코딩**, 스냅 그리드는 8px이었습니다. 서로 절대 맞을 수 없는 두 격자를 동시에 보여주고 있었던 셈입니다. 체크무늬 한 칸 = 그리드 한 칸으로 묶었습니다(간격이 6px 미만이면 칸이 뭉개지므로 정수 배로 키웁니다 — 모서리는 여전히 격자 위).

점은 `cx=0.5, cy=0.5, r=0.5`라 모서리에 **걸친 게 아니라 안쪽에 붙어** 있어서 확대하면 어긋나 보였습니다. 칸 **가운데**로 옮기고, 체크무늬가 이미 같은 칸을 보여주는 투명 배경에서는 점 레이어를 그리지 않습니다. 흰색·종이 배경에서만 체크무늬 대신 나옵니다.

### 연결선 라벨 정렬

`asStyle`이 `align: 'center'`로 **박아** 두고 있었습니다. `EdgeStyle.align`(옵션)을 추가해 기준점 대비 왼쪽·가운데·오른쪽을 고를 수 있게 했습니다. 라벨 배경판도 같은 앵커 규칙을 읽어야 합니다 — 안 그러면 글자만 움직이고 판은 가운데에 남습니다.

## 2026-08-11 — AI Figure Maker: 직각 라우터가 자기 노드를 관통하던 문제

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/aifig/resolve.ts`, `src/tools/aifig/geometry.test.ts`
- 요약: residual(skip) 연결선이 위로 한 바퀴 돌아 출발 노드를 뚫고 지나가던 것을 고쳤습니다.

같은 면으로 나가서 같은 면으로 들어가는 연결선 — 잔차 연결의 기본형입니다 — 은 목표가 stub 뒤쪽에 있습니다. 이때 후보 목록에 **한 번 꺾는 경로가 없었습니다.** 곧바로 우회 lane(`zy(laneY)`)으로 떨어지는데, lane은 양 끝점을 **바깥에서 감싸는** 자리라 반대편으로 가려면 방금 떠난 노드를 가로지르게 됩니다. 게다가 두 lane 중 어느 쪽을 고르는지가 중점과의 거리 비교라, 끝점이 멀면 사실상 동전 던지기입니다(실측에서 209 대 210으로 위쪽 lane이 이겼습니다).

`zy(e.y)`(수직축 평행일 때는 `zx(e.x)`)를 우회 lane보다 **먼저** 넣었습니다. stub이 이미 연 통로로 내려가 목표의 행을 따라 들어오는 한 번 꺾기이고, 두 도형 바깥에 머무릅니다. 실측: `M100 137.5 → 위로 115.5 → 오른쪽 170 → 아래로 509`(170은 출발 박스 100~400 **안쪽**)에서 `M100 137.5 → L90 137.5 → 아래로 509 → L188 517`로 바뀌었습니다.

안전망으로 `route(..., avoid)`를 뒀습니다. `resolveEdge`가 양 끝 노드의 박스를 넘기면 라우터가 그 안을 지나는 후보를 1차로 걸러냅니다. **두 번째 패스는 avoid를 무시합니다** — 자리가 정말 좁을 때 겹침 대신 라운드 코너를 포기하는 쪽이 낫기 때문입니다. `anchor: 'c'`와 자유 끝점은 애초에 안쪽에서 시작하므로 제외합니다.

## 2026-08-11 — AI Figure Maker: 연결선 붙는 위치

- 변경 파일: `src/tools/aifig/Inspector.tsx`
- 요약: 연결선의 시작·도착이 노드의 **어느 면에 붙을지** 고르는 컨트롤을 넣었습니다.

"오른쪽에서 나와서 위로 들어가는" 흔한 L자 분기를 GUI로 만들 방법이 없었습니다. 라우팅 기하(`geometry.ts`)는 처음부터 9방향 앵커를 전부 지원했는데 — `UNIT`·`NORMAL`·`ANCHOR_KEYS`가 다 있습니다 — **노출하는 UI가 없었습니다.** 앵커 점에서 끌면 *시작* 면만 정해지고, 도착은 `connectNodes(..., 'auto')`로 **코드에 박혀** 있었습니다. 즉 조작 미숙이 아니라 표현 불가능한 조합이었습니다.

인스펙터 연결선 패널에 `시작: / 끝:` 선택을 추가했습니다. 자동·위·오른쪽·아래·왼쪽 다섯 개만 냅니다 — 모서리(ne/nw/se/sw)는 라우팅이 지저분해지고 `auto`가 그 자리를 대신합니다.

시작 `오른쪽` + 끝 `위` + 경로 `직각`이면 `M240 72 → L382 72 → 코너 → L390 210` 같은 깔끔한 L이 나옵니다. 끝점이 `{free}`(노드에 안 붙은 자유 끝)인 경우 패치를 건너뛰므로 안전합니다.

## 2026-08-11 — AI Figure Maker: 색 팝오버 잘림, 복사/붙여넣기

- 변경 파일: `src/tools/aifig/ui.tsx`, `src/tools/AiFigureMaker.css`, `src/tools/AiFigureMaker.tsx`
- 요약: 인스펙터가 색 팝오버를 잘라내던 문제와, 노드를 복사해도 예전 이미지가 붙던 문제를 고쳤습니다.

### 팝오버는 스크롤 컨테이너 안에 두면 안 됩니다

`.af-color__pop`이 스와치 안에 `position: absolute`로 있었고, `.fx-insp__body`가 `overflow-y: auto`입니다. **overflow 컨테이너는 스크롤 축뿐 아니라 사방에서 자식을 잘라냅니다.** 그래서 패널 왼쪽에 붙은 스와치는 팝오버 왼쪽 절반이 통째로 잘렸습니다.

`createPortal`로 도구 루트(`.femtool`)에 내보내고 `position: fixed` + 컴포넌트가 좌표를 계산하도록 바꿨습니다. **`<body>`가 아니라 `.femtool`인 이유는 CSS가 `.scope-aifig` 아래로 스코프돼 있기 때문입니다** — body로 보내면 스타일이 전부 날아갑니다.

딸려오는 것 세 가지: 바깥 클릭 판정이 이제 버튼과 팝오버를 **둘 다** 확인해야 합니다(팝오버가 더 이상 버튼의 자손이 아님). 위치 재계산은 `scroll` 리스너를 `capture: true`로 달아야 인스펙터 스크롤을 잡습니다. 그리고 좌표는 뷰포트 안으로 clamp하고, 아래 공간이 모자라면 버튼 위로 뒤집습니다.

⚠️ **상태 업데이터 안에서 이벤트를 읽으면 안 됩니다.** `setHost((h) => ... e.currentTarget.closest(...))`로 짰다가 터졌습니다 — React가 렌더 중에 업데이터를 다시 호출하는데 그때 `currentTarget`은 이미 null입니다. 핸들러 본문에서 먼저 꺼내 변수에 담아야 합니다.

### ⌘C가 시스템 클립보드를 가져가지 않았습니다

노드를 복사한 뒤 붙여넣으면 **몇 시간 전에 복사해둔 이미지가** 들어왔습니다.

`copySel()`은 메모리 버퍼(`clipRef`)만 채웠고, 붙여넣기 핸들러는 이미지를 먼저 검사합니다. OS 클립보드는 갱신된 적이 없으니 낡은 이미지가 영원히 이깁니다. 결정적으로 ⌘C keydown이 **`preventDefault()`를 호출해서 네이티브 `copy` 이벤트 자체를 막고 있었습니다** — 클립보드를 가져갈 유일한 통로를 스스로 닫아둔 셈입니다.

keydown의 ⌘C 처리를 걷어내고 `copy` 리스너에서 `clipboardData.setData('text/plain', …)`으로 태그된 JSON을 씁니다. `copy` 이벤트에서의 `preventDefault()`는 "데이터를 내가 직접 썼다"는 뜻이라 이건 맞습니다. 붙여넣기 우선순위는 **내 페이로드 → 이미지 → 메모리 버퍼** 순입니다. 덤으로 창 두 개 사이 복사가 됩니다.

커스텀 MIME 대신 `text/plain`을 쓴 이유는 그것만 브라우저와 OS 클립보드를 온전히 통과하기 때문입니다. 태그(`funky-esset-maker/aifig-nodes`)가 일반 텍스트 붙여넣기를 도형으로 오해하는 걸 막습니다.

## 2026-08-10 — AI Figure Maker: 연결 모드와 층 전결합

- 변경 파일: `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/{doc.ts,templates.ts,Overlay.tsx}`, `src/tools/aifig/doc.test.ts`(신규)
- 요약: 커넥터를 **클릭으로** 만들 수 있게 하고, 선택한 층들을 한 번에 전결합하는 동작과 낱개 뉴런 층 템플릿을 넣었습니다.

### 화살표를 못 만들던 진짜 이유

`ToolMode`가 `types.ts`에 `'select' | 'connect' | 'pan' | {insert}`로 선언돼 있는데 **에디터가 한 번도 읽지 않습니다.** 연결 모드는 타입에만 있었고 구현이 없었습니다. 그래서 간선을 만드는 경로가 하나뿐이었습니다 — 노드에 마우스를 올렸을 때만 나타나는 반지름 4px짜리 초록 점에서 **끌기**. 힌트 바에 "초록 점에서 끌면 연결"이라고 적혀 있긴 했지만, 알기 전에는 클릭만 해서는 아무 일도 일어나지 않습니다.

클릭 연결(`connecting` 상태)을 넣었습니다. 시작 노드를 누르고 도착 노드를 누릅니다. **시작점은 연결 후에도 무장 상태로 남습니다** — 한 노드에서 다음 층 전체로 뻗는 fan-out이 간선당 클릭 한 번이 되기 때문입니다. Esc는 한 단계씩 빠져나옵니다(시작점 해제 → 모드 종료).

### ⚠️ 끌기 방식이 애초에 불가능했던 이유 — 호버를 DOM에 맡기면 안 됩니다

남아 있던 끌기 경로도 사실 **동작하지 않았습니다.** 점에 다가가면 점이 사라집니다.

앵커 점은 노드 테두리 **위에** 놓이고 `pointerEvents="all"`이며, 히트 사각형과 **다른 `<g>`에 있습니다.** 호버는 히트 사각형의 `pointerenter`/`pointerleave`로 정했었는데, 점에 닿는 순간 최상위 히트 대상이 점으로 바뀌고 점은 사각형의 자식이 아니므로 사각형에 `pointerleave`가 발생합니다 → `hoverId=null` → **지금 잡으려던 점이 언마운트** → 포인터가 다시 사각형으로 떨어져 `pointerenter` → 점 재생성. 깜빡이기만 하고 절대 못 잡습니다.

호버를 **좌표로** 판정하도록 바꿨습니다(`onHoverMove`). 캔버스 `svg`의 `onPointerMove`에서 문서를 역순으로 훑어 `pointInNode(world, n, 9/zoom)`으로 맞춥니다. 어떤 DOM 요소가 위에 있든 무관해집니다.

**허용 오차 `9/zoom`은 줄이면 안 됩니다.** 점은 테두리에 걸쳐 있어 반지름만큼 박스 **바깥으로 튀어나옵니다**(현재 5.5px). 허용 오차가 그보다 작으면 점의 바깥 절반에서 호버가 풀려 같은 증상이 돌아옵니다. 점 크기를 키우면 이 값도 같이 키워야 합니다.

### 전결합이 없으면 손으로 못 그린다

이번 요청의 출발점이 완전연결 그림을 직접 그리는 것이었는데, 3·4·4 세 층이면 간선이 28개입니다. 끌기로 28번은 아무도 안 합니다. `fullyConnect`(`doc.ts`)는 선택된 노드를 **중심 x의 간격**으로 열로 나눈 뒤 이웃한 열끼리 모두 잇습니다. 층 안의 유닛은 x가 같으니 의미 있는 간격은 전부 층 경계입니다.

함정 두 개를 테스트로 박아뒀습니다(`doc.test.ts`).

- **컨테이너를 유닛으로 세면 안 됩니다.** 층 배경 박스는 마퀴 선택에 딸려 오는데, 그대로 두면 유닛마다 그럴듯한 선이 하나씩 더 생겨서 눈으로는 안 잡힙니다. 다른 선택 노드를 완전히 감싸는 노드는 먼저 버립니다.
- **세 열을 고르면 1↔3은 잇지 않습니다.** 이웃한 열만 연결합니다.

생성된 간선은 **화살촉 없이** 만들고 곧바로 선택 상태로 둡니다. 이 밀도에서 시냅스마다 화살촉을 다는 도면은 없고, 선택돼 있으니 스타일 일괄 변경이 인스펙터 한 번입니다.

### `mlp` 글리프로는 부족한 경우

기존 `mlp` 도형은 네트워크 전체를 **한 덩어리**로 그립니다. 개략도로는 맞지만 뉴런 하나를 색칠하거나 특정 연결만 지우려는 순간 쓸 수 없습니다. `뉴런 층 3개 (3·4·4)` / `뉴런 층 1개 (4)` 템플릿은 같은 그림을 **낱개 노드**로 놓습니다 — 층마다 둥근 컨테이너 + 타원 유닛, 층 단위로 그룹. 떨어뜨리고 ⌘A → L이면 끝입니다.

단축키는 `C`(연결 모드), `L`(전결합)입니다. 둘 다 수정키 없는 글자라 기존 빠른 삽입(r·e·t·b·m·o)과 겹치지 않는지 확인했습니다.

## 2026-08-09 — 도구 크롬을 한 벌로 통일

- 변경 파일: `src/tools/shell.css`·`BgPicker.tsx`·`bg.ts`(신규), 도구 CSS 9개 + `tool-sources/*/App.css` 5개, `src/tools/{Grapher,AiFigureMaker,Tabler,…}.tsx`
- 요약: 열 개 도구가 각자 복사해 갖고 있던 툴바·스테이지·에디터·힌트·토스트를 **한 파일로 모으고**, 그 과정에서 이미 벌어져 있던 차이를 없앴습니다. 도구 CSS에서 892줄이 빠졌습니다.

### 왜 파일을 합쳤나

도구들이 원래 별개 프로젝트였던 탓에 같은 크롬을 열 벌 갖고 있었고, 이미 어긋나 있었습니다 — Highlighter만 스테이지가 점 격자였고, 토스트가 세 가지 높이에 있었고, Grapher만 툴바가 캔버스 위에 떠 있었습니다. 값이 95% 같았으니 고쳐도 됐지만, 그러면 다음 기능에서 다시 벌어집니다.

`src/tools/shell.css`가 `.femtool` 아래에서 크롬을 소유하고, 도구 CSS에는 **`.shot` 안의 그림과 그 도구에만 있는 컨트롤만** 남깁니다.

⚠️ **shell.css는 도구 CSS보다 먼저 로드되어야 합니다.** 선택자 명시도가 같아서 나중 것이 이깁니다. App.tsx에서 import하는데(메인 청크), 도구는 lazy라 CSS가 뒤에 붙습니다. **도구에서 import하면 안 됩니다.**

### 없앤 차이들

- **`.shot`의 테두리·그림자를 뺐습니다.** Cartesian·Chart·Number Line만 그림을 3px 검정 액자에 넣어 보여줬는데, 그 액자는 내보낸 파일에 없습니다. 논문 모드에서 특히 나빴습니다 — 헤어라인 도면이 굵은 검정 상자 안에 들어앉았습니다. `.shot`은 내보내기 대상이므로 화면에만 있는 장식을 붙이면 안 됩니다.
- **토스트를 `.stage` 안으로 옮겼습니다.** `position: fixed`에 도구마다 다른 bottom 값(64/96/24/46)을 쓰고 있었는데, 아래 크롬 높이가 바뀔 때마다 같이 고쳐야 하는 숫자였습니다. 이제 스테이지 기준 20px 하나입니다.
- **Grapher 툴바를 상단 바로 바꿨습니다.** 유일하게 캔버스 위에 떠 있었습니다. 캔버스와 그 위에 뜨는 패널(줌·인스펙터·토스트)은 `.canvasbox`로 묶었습니다 — 패널을 `.canvas` 안에 넣으면 패널 클릭이 화면 이동으로 잡힙니다.
- **AI Figure Maker의 상태줄을 힌트 바로** 내리고, 체커보드 색·간격과 툴바 패딩을 표준에 맞췄습니다. 내보내기 버튼 색도 다른 도구와 같게 (주황=벡터/소스, 초록=PNG, 하늘=복사).
- **배경 선택을 스와치로.** 여섯 도구가 똑같은 4개 배열과 4개 텍스트 버튼을 갖고 있었고 툴바에서 230px씩 먹었습니다. `BgPicker` 하나로 합치니 Number Line·Truth Table은 툴바가 한 줄로 줄었습니다. 투명은 스테이지 체커보드의 축소판으로 그립니다.
- 툴바 그룹 사이에 구분선(`.toolbar__group + .toolbar__group`)을 넣었습니다. AI Figure Maker가 이미 쓰던 스타일을 그대로 가져왔습니다.

## 2026-08-09 — 논문 모드 · 벡터 내보내기 · 실행 취소 · 인스펙터

- 변경 파일: `src/theme.ts`·`src/paper.css`·`src/tools/{svg,paper,tex,Inspector,PrintBar,UndoRedo}.*`·`src/cores/figure.ts`(신규), `src/tools/hooks.ts`, `src/cores/Chart.tsx`, 도구 10개 전부, `README.md`·`public/llms.txt`·`.claude/skills/esset/SKILL.md`
- 요약: AI Figure Maker에만 있던 세 가지(벡터 렌더·실행 취소·인스펙터)를 다른 도구로 옮기고, **기존 디자인을 그대로 둔 채** 논문용 출력을 뽑는 모드를 추가했습니다.

### 논문 모드를 문서가 아니라 렌더 모드로 만든 이유

`theme: 'funky' | 'paper'`를 **앱 레벨**에 두고 context로 내려보냅니다. 도구마다 토글을 다는 대신 하나로 둔 건, 이게 "오늘 뭘 만드는가"에 대한 답이지 표와 그래프에서 따로 정할 문제가 아니기 때문입니다.

핵심은 **색 선택을 지우지 않는다**는 것입니다. 논문 모드는 헤더색·강조색·T/F 색칠을 *칠하지 않을 뿐* 값은 문서에 남겨 둡니다. 펑키로 돌리면 그대로 복원됩니다. 같은 표를 슬라이드용과 논문용으로 두 번 만들 필요가 없는 게 이 설계의 전부입니다.

⚠️ **HTML 도구의 색은 CSS가 아니라 도구 코드에서 꺼야 합니다.** Tabler·Truth Table·DS Visualizer는 헤더/강조색을 inline `style`로 넣기 때문에 CSS 오버라이드로는 `!important` 없이 못 이깁니다. 논문 모드에서는 애초에 inline style을 만들지 않는 쪽으로 갔습니다. `paper.css`는 테두리·폰트·그림자만 담당합니다.

booktabs의 mid rule은 `tr:first-child`로 잡지 않고 도구가 `cell--midrule` 클래스를 붙입니다 — "첫 행"과 "헤더 행"은 헤더 *열*만 켜져 있을 때 다른 것이라서요.

### 벡터 내보내기 — 함정은 CSS 변수였습니다

`src/tools/svg.ts`가 화면의 `<svg>`를 복제해 독립 문서로 직렬화합니다(다시 그리지 않습니다. 두 번째 렌더러는 사용자가 승인한 것과 어긋날 수 있는 두 번째 물건입니다). mm 단위 물리 크기를 써 넣어 `\includegraphics`가 단 폭에 정확히 앉습니다. `aifig/export.ts`의 `svgToPng`·sRGB 태깅·`download`는 이쪽으로 옮기고 재수출했습니다.

⚠️ **독립 SVG에는 스타일시트도 `:root`도 없습니다.** Cartesian·Number Line·Chart가 `<text>`에 `fontFamily="var(--mono)"`를 쓰고 있었는데, 화면에서는 멀쩡하고 내보낸 파일에서는 전부 브라우저 기본 세리프로 떨어집니다. 폰트 스택을 `cores/figure.ts`에 상수로 박았고, 테스트에서 `var(`가 없는지 확인합니다. **내보내는 SVG 마크업에 CSS 변수를 다시 넣지 마세요.**

선택 링·투명 히트 영역 같은 편집기 전용 요소는 `data-ui` 속성을 달아 내보낼 때 제거합니다. 얇은 논문 선(1.4px 곡선)은 그냥은 클릭이 거의 안 돼서 히트 영역이 필수인데, 그게 파일에 남으면 안 되니까요.

### 인쇄 폭과 pt — 조용히 틀리는 종류의 버그

단 폭 프리셋만 고르면 안 됩니다. 760px로 그린 그림을 3.25인치로 인쇄하면 라벨이 4pt가 되고, 파일은 완전히 정상이며, PDF가 조판되어 돌아올 때까지 아무도 모릅니다. 그래서 프리셋을 고르면 **캔버스 픽셀도 같이 바꾸고**(1인치=96px), 툴바에 실제 인쇄 pt를 띄워 6pt 미만이면 빨갛게 만듭니다. `PrintBar`가 이 두 가지를 한 컴포넌트로 묶은 이유입니다.

### 표와 코드는 이미지로 내보내지 않는 게 낫습니다

`tabler`·`truth`는 booktabs `.tex`를, `highlighter`는 `listings` 블록을 내보냅니다. 논문에서 표 이미지는 본문 폰트와 어긋나고 검색도 안 됩니다. 이쪽이 SVG보다 훨씬 싸고 결과도 낫습니다.

⚠️ `escapeTex`는 `.replace()` 체인이면 안 됩니다. `\` → `\textbackslash{}` 치환이 만든 중괄호를 뒤 규칙이 다시 이스케이프해서 `\textbackslash\{\}`가 나옵니다. 룩업 테이블 + 단일 정규식 한 번으로 처리합니다 — 자기 출력을 다시 훑지 않습니다. (테스트가 잡았습니다.)

### 실행 취소

`useHistory(persisted, restore)` — 도구가 이미 localStorage에 보내는 객체를 그대로 감시하고, 스냅샷을 `restore`로 되돌려 줍니다. 도구를 reducer로 뜯어고치는 것보다 도구당 6줄이 쌉니다.

⚠️ **텍스트 필드에 포커스가 있으면 ⌘Z를 가로채지 않습니다.** textarea 안에서 사용자가 의도하는 건 브라우저의 되돌리기이고, 그걸 뺏으면 타이핑이 고장 난 것처럼 느껴집니다. 툴바 버튼은 항상 동작합니다.

스택은 ref, 깊이는 state로 미러링합니다 — 렌더 중에 ref를 읽으면 concurrent rendering에서 깨집니다.

Grapher는 원래 자기 히스토리(nodes+edges 스냅샷)가 있어서 그대로 두고 버튼만 공유합니다. 화살표는 `↶ ↷` 대신 인라인 SVG입니다 — Pretendard에 그 코드포인트가 없어 두부가 나옵니다.

### 인스펙터

`Inspector.tsx`는 행만 받아 그리는 얇은 패널이고, 도구마다 "무엇이 선택되었는가"가 달라서 그 이상은 모릅니다. Cartesian은 이미 graphs 패널이 인스펙터 역할을 하고 있어서 새 패널을 겹치지 않고, 곡선을 클릭하면 해당 행이 강조되게만 했습니다.

Number Line·Chart Maker에서 인스펙터 편집은 **소스 텍스트의 그 줄을 다시 쓰는 방식**입니다. 텍스트가 계속 문서이고 인스펙터는 다른 입력 방법일 뿐이라, 두 편집 경로가 어긋날 수 없습니다.

## 2026-08-09 — 프로젝트 파일 포맷 · AI 입력 경로 · UX 정리

- 변경 파일: `src/project.ts`·`project.test.ts`(신규), `public/llms.txt`(신규), `.claude/skills/esset/SKILL.md`(신규), `src/App.{tsx,css}`, `src/index.css`, `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/doc.ts`, 도구 9개(툴팁)
- 요약: 도구별로 없던 저장/열기를 **하나의 JSON 포맷**으로 만들고, 그 포맷을 AI가 쓸 수 있게 문서화했습니다. 열기·저장·내보내기를 모든 도구에서 같은 자리·같은 키로 통일했습니다.

### 프로젝트 파일: 도구를 하나도 건드리지 않고 만든 이유

봉투는 `{ app, format, tool, savedAt, data }`이고 `data`는 **그 도구가 localStorage에 쓰던 상태 그대로**입니다. 이 선택이 전부를 결정했습니다 — 저장은 도구가 이미 쓰고 있는 키를 앱이 읽으면 되고, 불러오기는 그 키에 써넣고 도구를 remount하면 끝입니다. 도구 10개 중 어느 것도 import/export 코드를 갖지 않고, 따라서 어느 것도 포맷과 어긋날 수 없습니다.

`tool` 필드 덕에 "어느 도구 파일인지" 사용자가 기억할 필요가 없습니다. 아무 도구에서나 열면 주인 도구로 전환됩니다. 창 아무 데나 드래그해도 됩니다.

⚠️ **`project.ts`의 `STORE_KEYS`는 호환성 표면입니다.** 키 하나를 바꾸면 저장된 파일 전부와 모든 사용자의 진행 중인 작업이 고아가 됩니다. 테스트에 값을 통째로 박아 두었으니, 바꿔야 한다면 마이그레이션을 같이 써야 합니다.

**`loadDoc`이 `normalizeDoc`을 거치게 바꿨습니다.** 예전에는 파일로 열 때만 정규화하고 localStorage 경로는 날것을 믿었는데, 이제 불러오기가 localStorage를 거치므로 그 구멍으로 손으로 쓴(또는 AI가 만든) 부분적 JSON이 그대로 들어옵니다.

### AI가 만들 수 있게

`data`의 빠진 필드가 전부 기본값으로 채워지므로, AI가 표·그래프·figure를 JSON으로 써 주면 사용자는 열기만 하면 됩니다. `public/llms.txt`(배포 시 `/llms.txt`)에 도구별 스키마·입력 문법·예시를 적었고, `.claude/skills/esset/`에 스킬도 뒀습니다.

**llms.txt의 예시 10개를 실제로 드롭해 전부 열리는 것을 확인했습니다.** 문서의 예시가 동작하지 않으면 문서가 없느니만 못하므로, 예시를 고칠 때마다 같은 확인을 해야 합니다. 사이드바 푸터의 "AI로 만들기" 링크가 `/llms.txt`를 가리킵니다 — 이 입력 경로는 알려주지 않으면 아무도 발견하지 못합니다.

### 정형화와 UX

- 열기·저장을 각 도구 툴바가 아니라 **사이드바**에 뒀습니다. 도구를 바꿔도 자리가 그대로고, 가뜩이나 빽빽한 툴바에 줄을 더하지 않습니다. AI Figure Maker에 있던 자체 `열기`/`.json` 버튼은 제거했습니다(SVG·PNG·TikZ만 남김) — 같은 일을 하는 버튼이 두 군데 있을 이유가 없습니다.
- 단축키: `⌘O` 열기 · `⌘S` 저장 · `⌘E` PNG · `⌘⇧C` 복사. 앞의 둘은 App에, 뒤의 둘은 `usePngExport` 안에 넣어 8개 도구에 한 번에 붙었습니다. AI Figure Maker만 자체 내보내기라 따로 달았는데, ⌘C가 이미 노드 복사라 **⌘⇧C를 위해 기존 핸들러에 `!e.shiftKey` 가드를 넣어야 했습니다.**
- 앱 토스트를 우하단으로 옮겼습니다. 도구 토스트가 전부 하단 중앙(24~96px)이라 프로젝트 메시지와 내보내기 메시지가 겹쳤습니다.
- **포커스 링을 전역으로 넣었습니다.** funky-ui가 press와 lift는 스타일링하지만 focus는 하지 않아서, 키보드로 다니면 자기 위치를 볼 수 없었습니다. 작은 컨트롤이 빽빽한 도구라 실제로 못 쓸 수준이었습니다.

## 2026-08-09 — 코드 평가 후 정리: 중복 제거 · 린트 0 · 테스트/CI 도입

- 변경 파일: `src/tools/hooks.ts`·`NumField.tsx`(신규), 도구 8개 전부, `src/cores/*`, `src/tools/aifig/geometry.ts`, `src/App.{tsx,css}`, `tsconfig.{app,node}.json`, `.github/workflows/ci.yml`(신규), `README.md`
- 요약: 저장소 전반을 평가하고 거기서 나온 항목을 전부 처리했습니다. 소스 약 900줄이 줄었고, 린트 43건 → 0건, 테스트·CI가 생겼습니다. 그 과정에서 **커넥터 대각선 버그**와 **Tabler 내보내기 30초 문제**를 찾아 고쳤습니다.

### ⚠️ `dedupe`의 허용 오차를 다시 키우면 대각선이 재발합니다

`geometry.ts`의 `dedupe`가 0.5px 이내로 붙은 두 점을 같은 점으로 보고 버리고 있었습니다. 문제는 그 0.5px가 **없어도 되는 오차가 아니라 실제로 필요한 이동**이라는 점입니다. 두 노드는 픽셀 단위로 정렬돼 있는 경우가 드물어서, ortho 경로에는 긴 가로 구간 두 개 사이에 0.25px짜리 세로 구간이 끼는 일이 흔합니다. 그 점을 버리면 오프셋이 사라지는 게 아니라 **양옆 구간으로 옮겨가고, 직각이어야 할 선이 대각선이 됩니다.**

2026-08-07에 "대각선을 없앴다"고 기록했지만 이 경로로 남아 있었습니다. 그때는 `roundCorners`의 `continue` 버그만 잡았고, 그 앞단계인 `dedupe`는 보지 않았습니다. 허용 오차를 0.01px로 낮춰 미세 구간을 살립니다 — 짧은 구간은 `roundCorners`가 하드 코너로 처리하므로 눈에 보이지 않습니다.

**이 버그는 눈으로 찾을 수 없습니다.** 노드 간격 4~220px를 훑는 3025개 조합 중 어긋나는 건 아주 좁은 간격 몇 개뿐이라, 한 쌍만 보면 멀쩡합니다. 그래서 스윕 테스트를 `geometry.test.ts`로 남겼습니다. 같은 스윕에서 코너의 89.4%가 온전한 반지름을 유지합니다(수정 전 88.8%).

### ⚠️ Tabler는 PNG에 웹폰트를 심지 않습니다

Tabler만 `skipFonts: false`였습니다 — 셀 글자에 본문 폰트를 유지하려던 의도인데, 대가가 컸습니다. 본문 스택의 첫 항목인 Pretendard는 CDN에서 오고, html-to-image가 그 CSS를 받아 **모든 웨이트(woff/woff2 18개, 한글이라 수십 MB)를 매 내보내기마다 base64로 인라인**했습니다. 캐시가 없는 첫 내보내기는 30초가 넘었고 그다음도 2~4초였습니다. 다른 도구는 0.25초입니다.

LaTeX Imager가 쓰는 방법(필요한 폰트만 골라 인라인)을 그대로 쓸 수 없습니다. 그건 `document.styleSheets`를 순회하는데, **Pretendard는 cross-origin 시트라 규칙을 읽을 수 없습니다.** KaTeX는 same-origin이라 되는 것뿐입니다.

폰트 스택의 폴백(Apple SD Gothic Neo · Noto Sans KR · Malgun Gothic)이 한글을 제대로 렌더하므로 시스템 폰트로 내보내기로 했습니다. 205ms / 102ms로 떨어졌습니다. 대가는 화면과 PNG의 자형이 미세하게 다르다는 것 — 자형을 정확히 맞춰야 하면 CDN CSS를 직접 fetch해 필요한 웨이트(400·700·800)만 인라인하는 방법이 남아 있습니다.

### 공통 로직을 `hooks.ts`로

도구 8개가 각각 독립 프로젝트였던 탓에 `loadState` / `withExport` / `flash` / preview fit / `savePng` / `copyPng`를 저마다 복제해 갖고 있었습니다. 실질 차이는 pixelRatio와 파일명뿐이라 `useStored` · `usePersist` · `useFitScale` · `usePngExport`로 묶었습니다. `NumField`(3개 도구에 복제)도 함께 뺐습니다. 소스 1170줄이 지워지고 273줄이 들어왔습니다.

`usePngExport`에는 확장점 두 개를 뒀습니다 — `guard`(내보내기 전 거부 사유, LaTeX Imager·DS Visualizer가 사용)와 `options`(내보내기마다 계산되는 html-to-image 옵션, LaTeX Imager의 KaTeX 폰트 CSS).

### ⚠️ AI Figure Maker의 `docRef`는 `useLatest`로 바꾸면 안 됩니다

렌더 중 ref에 대입하던 곳(`docRef.current = doc` 등)을 정리하면서, `viewRef`·`selNodesRef` 같은 읽기 전용 미러는 `useLatest`(effect에서 대입)로 옮겼습니다. **`docRef`만 예외입니다.** `commit`/`live`/`undo`/`redo`가 `setDocState` 직전에 `docRef.current`를 직접 갱신하는데, 이게 있어야 같은 틱 안에서 이어지는 편집이 앞의 결과 위에 쌓입니다. `useLatest`는 커밋 이후에 쓰므로 한 프레임 늦고, 연속 편집이 서로를 덮어씁니다.

undo/redo 버튼의 활성 상태는 렌더 중 `hist.current.past.length`를 읽고 있었습니다. Grapher도 같은 패턴이었고요. 두 곳 다 스택은 ref에 두되(드래그 중 push마다 리렌더하면 안 되므로) 버튼이 쓰는 boolean 두 개만 state로 미러링합니다.

### cores는 3개 중 1개만 컴포넌트째로 붙습니다

`src/cores/`(CodeBlock · Diagram · Chart)를 아무도 import하지 않아 검증 없이 표류하고 있었습니다. 앱에서 실제로 쓰기로 했는데, 붙여보니 **Chart만 됩니다.** Chart Maker의 렌더 부분은 순수 표시라 `<Chart/>`로 교체돼 608 → 289줄이 됐습니다. 반면 Diagram의 짝은 Grapher, CodeBlock의 짝은 Highlighter인데 **둘 다 편집기**입니다 — 노드마다 이벤트·선택·ref가 필요하고, 표시 전용 컴포넌트를 그렇게 확장하면 그건 이미 그 도구입니다.

그래서 남은 둘은 **어긋나면 안 되는 부분만** 공유합니다: Prism 하이라이트 단계(`cores/highlight.ts` ← Highlighter)와 노드 팔레트(`cores/palette.ts` ← Grapher). 색이 갈리면 Grapher 편집 화면과 슬라이드에 렌더된 Diagram이 달라지는데, 이제 한 곳에서 옵니다. 팔레트를 컴포넌트 파일이 아니라 별도 파일에 둔 이유는 Fast Refresh입니다 — 컴포넌트와 상수를 같이 export하면 refresh 경계가 깨집니다.

### 그 밖에

- **`strict`가 아예 꺼져 있었습니다.** Vite 템플릿 기본값인데 tsconfig 어디에도 없었습니다. 켜고 돌려보니 **에러 0건** — 이미 strict를 만족하는 코드였고 플래그만 빠져 있었습니다.
- 린트 43건은 대부분 `eslint-plugin-react-hooks` v7의 새 규칙이었습니다. `useMemo(loadState, [])` 8건은 훅 추출로 사라졌고, 렌더 중 ref 접근 15건은 위의 방식으로, prop→state 동기화 effect는 렌더 중 조정(공식 권장 패턴)으로 바꿨습니다. 억제 주석을 남긴 곳은 Highlighter의 스크롤 clamp 한 곳뿐입니다 — DOM 측정 결과라 렌더 중에 구할 수 없고 paint 전에 반영돼야 합니다.
- 반응형: 미디어 쿼리가 0개였습니다. 앱 사이드바는 1100px 미만에서 아이콘 레일(58px)로, AI Figure Maker의 좌우 패널은 1280px 미만에서 접힌 채로 시작합니다. 접힘 상태는 접힌 패널이 자기 펼침 버튼 폭(34px)만 남기는 방식이라 컨트롤이 화면 밖으로 나가지 않습니다. 사이드바 선택은 `localStorage`에 남고, 명시적 선택이 폭 휴리스틱을 이깁니다.
- CI(`.github/workflows/ci.yml`)는 lint → test → build 순으로 돌고, 마지막에 `gen:css`를 실행해 **생성된 CSS 5개**가 커밋본과 같은지 검사합니다. 생성물을 직접 고치면 다음 `gen:css`에 조용히 지워지는데, 그걸 CI가 잡습니다. `AiFigureMaker.css`와 `ChartMaker.css`는 손으로 쓰는 파일이라 검사 대상이 아닙니다.

미해결로 남긴 것: `npm audit`에 postcss 관련 high 2건이 있습니다(빌드 도구 전이 의존성, 런타임 아님).

## 2026-08-07 — 저장소 하나로 정리 (독립 도구 5개 아카이브)

- 대상: 로컬 `~/Projects/Main Projects/funky-essets/`, GitHub `bsiku3622/funky-essets-*` 5개
- 요약: 앱을 `funky-essets/` 루트로 승격하고, 완전히 중복이던 독립 도구 저장소 5개를 아카이브했습니다.

정리 전에는 `funky-essets/` 아래에 폴더 6개가 나란히 있고 각각이 별도 git 저장소였습니다. 조사해 보니 **독립 도구 5개(Highlighter · LaTeX Imager · Tabler · DS Visualizer · Grapher)의 소스가 통합 앱 안의 복사본과 완전히 동일**했습니다 — `App.tsx` 줄 수까지 일치하고 CSS도 바이트 단위로 같았습니다. 마지막 커밋은 2026-06-09/10이고, Vercel에 배포된 것은 통합 앱 하나뿐이었습니다. 즉 죽은 중복이었습니다.

- 로컬: `Funky Esset Maker/`의 내용을 `funky-essets/` 루트로 올리고 폴더를 없앴습니다. 독립 도구 5개는 `~/Projects/Archive/funky-essets-standalone/`로 옮겼습니다.
- GitHub: 5개 저장소의 description을 "Merged into bsiku3622/funky-esset-maker"로 바꾼 뒤 아카이브했습니다. 삭제가 아니라 아카이브라 초기 개발 히스토리는 남아 있고, 필요하면 되돌릴 수 있습니다.

**이 작업은 로컬 폴더 재배치일 뿐 저장소 내용은 건드리지 않았습니다.** `funky-esset-maker` 저장소는 원래부터 루트에 `src/`와 `package.json`을 두고 있었고, 그것이 로컬에서 한 단계 깊은 폴더에 체크아웃돼 있었을 뿐입니다. 그래서 GitHub 구조와 Vercel 연동·도메인은 아무 영향을 받지 않았습니다. 저장소 이름도 `funky-esset-maker` 그대로 뒀습니다 — 폴더명과는 어긋나지만, 이름을 바꾸면 리다이렉트와 연동을 다시 확인해야 해서 얻는 것에 비해 번거롭습니다.

`tool-sources/`가 이제 그 다섯 도구 CSS의 **유일한 원본**입니다. 예전에는 독립 프로젝트의 `src/App.css`를 복사해 오는 흐름이었는데, 원본이 아카이브됐으니 이제 `tool-sources/`에서 직접 고치고 `npm run gen:css`를 돌리면 됩니다.

### ⚠️ 폴더명을 바꿀 때는 Claude 기록도 같이 옮겨야 합니다

정리 직후 로컬 폴더명을 저장소 이름에 맞춰 `funky-essets` → `funky-esset-maker`로 바꿨습니다. 이때 폴더만 옮기면 **그동안의 Claude Code 세션 기록과 메모리가 끊깁니다.**

Claude Code는 프로젝트의 절대경로를 슬래시→하이픈으로 인코딩한 이름으로 `~/.claude/projects/` 아래에 기록을 둡니다. 폴더를 옮기면 새 경로에 대응하는 디렉토리가 새로 생기고, 옛 기록은 존재하지 않는 경로에 묶인 채 접근할 수 없게 됩니다. 그래서 폴더와 함께 그 디렉토리도 같은 규칙으로 rename해야 합니다:

```
~/.claude/projects/-Users-baeks-Projects-Main-Projects-funky-essets
  → ~/.claude/projects/-Users-baeks-Projects-Main-Projects-funky-esset-maker
```

이번에는 챙길 게 하나 더 있었습니다. 앱이 `funky-essets/Funky Esset Maker/`에 있던 시절의 기록 디렉토리(`…-funky-essets-Funky-Esset-Maker`)에 **`memory/`가 남아 있었습니다** — "명시적 요청 없이 커밋하지 말 것" 피드백이 거기 저장돼 있었고, 그대로 뒀으면 다시는 불러와지지 않았을 겁니다. 그 `memory/`도 새 디렉토리로 옮기고 빈 껍데기는 지웠습니다.

세션 도중에 옮겨도 안전합니다. `mv`는 inode를 유지하므로 지금 쓰이고 있는 로그 파일도 그대로 따라옵니다.

## 2026-08-07 — 이미지 반입 경로 + 캔버스 줌이 페이지를 확대하던 버그

- 변경 파일: `src/tools/aifig/image.ts` (신규), `src/tools/AiFigureMaker.tsx`, `src/tools/aifig/Inspector.tsx`, `src/tools/aifig/{types,shapes,doc}.ts`, `src/tools/AiFigureMaker.css`
- 요약: 드래그&드롭 / 붙여넣기로 비트맵을 바로 넣게 하고, 트랙패드 핀치가 브라우저 페이지를 확대하던 문제를 고침.

### ⚠️ 휠·핀치는 반드시 native non-passive 리스너로

React의 `onWheel`은 위임된 **passive** 리스너로 등록되기 때문에 그 안의 `preventDefault()`가 조용히 무시됩니다. 그래서 캔버스 위에서 트랙패드로 핀치하면 캔버스도 확대되고 **브라우저 페이지도 같이 확대**됐습니다. `stageRef`에 `addEventListener('wheel', fn, { passive: false })`로 직접 붙여야 막힙니다. Safari는 핀치에 대해 `gesturestart`/`gesturechange`를 따로 쏘므로 그것도 함께 막습니다.

**`onWheel` prop으로 되돌리면 버그가 그대로 재발합니다.** 겸사겸사 줌 스텝을 `Math.exp(-deltaY * 0.01)`로 바꿨습니다 — 핀치는 작은 델타가 연속으로 오는데 고정 10% 스텝이면 계단처럼 튑니다.

### 이미지

붙여넣기를 `keydown`이 아니라 **`paste` 이벤트**에서 처리합니다. keydown에서는 클립보드에 이미지가 있는지 알 수 없어서, `⌘V`가 이미지 붙여넣기와 노드 복제를 구분하지 못했습니다. paste 이벤트는 `clipboardData`를 주므로 "이미지가 있으면 이미지, 없으면 내부 노드 클립보드"로 갈라집니다. 스크린샷은 `clipboardData.files`가 아니라 `items`로 오기 때문에 양쪽을 다 봅니다.

비트맵은 data URL로 노드에 박습니다. SVG를 내보냈을 때 딸린 파일이 없어야 하기 때문인데, 대가로 문서가 무거워집니다. 그래서 (1) 긴 변 2400 px 초과분은 반입 시 리샘플하고, (2) `saveDoc`이 성공 여부를 돌려주게 해서 **localStorage 용량 초과를 토스트로 알립니다** — 예전에는 조용히 삼켜서 자동 저장이 멈춘 줄도 모르고 작업하게 됐습니다.

## 2026-08-07 — 직각 커넥터 라우팅 재작성

- 변경 파일: `src/tools/aifig/geometry.ts`, `src/tools/aifig/shapes.tsx`
- 요약: ortho 경로에 대각선이 섞이고 코너가 찌그러지던 문제를 고침. stack 도형이 자기 노드 박스와 어긋나 있던 것도 함께 수정.

세 가지가 겹쳐 있었습니다.

**1. 코너를 건너뛰면서 정점을 지나쳤다.** `roundCorners`가 필렛을 넣기엔 너무 좁은 코너를 만나면 `continue`했는데, 그때 cursor를 갱신하지 않아 **그 정점을 거치지 않고 다음 정점으로 직선을 그었습니다** — 직각이어야 할 경로에 대각선이 생기는 정체가 이것이었습니다. 이제 필렛이 불가능하면 하드 코너로 정점을 통과합니다. 겸사겸사 일직선 위의 정점에 필렛을 넣던 것도 없앴습니다(경로만 지저분해지고 시각적 차이는 없음).

**2. 코너가 짧은 구간에서 뾰족해졌다.** 반지름을 `min(r, d1/2, d2/2)`로 깎다 보니 두 노드가 가까우면 코너가 거의 직각이 됐습니다. ⚠️ 이제는 **반대 방향**입니다 — 반지름은 고정이고, 그만한 코너가 들어갈 자리가 없으면 *경로 쪽을 바꿉니다*. `orthoPoints`가 여러 후보(직행 Z, 위/아래 우회 레인, stub 연장)를 만들고 `legsFit`으로 "모든 구간이 양 끝 코너의 필렛을 담을 만큼 긴가"를 검사해 첫 합격 후보를 씁니다. 반지름 축소는 어떤 후보도 통과 못 할 때만 일어납니다. 노드 간격을 4~220px까지 훑는 3025개 조합에서 코너의 88.8%가 온전한 반지름을 유지합니다(나머지는 간격이 10px 미만인 배치).

`legsFit`은 "되돌아가는 구간"도 함께 거릅니다. 이전에는 경로가 목표점을 지나쳤다가 되돌아오면서 선이 자기 위에 겹치는 경우가 있었습니다.

**3. 마주보는 stub이 서로를 지나쳤다.** 고정 STUB(18px) 두 개가 노드 간격보다 길면 s와 e의 순서가 뒤집혀 이후 모든 라우팅이 꼬였습니다. 마주보는 경우에 한해 stub을 간격에 맞춰 대칭으로 줄입니다.

코너 곡선은 2차 베지어에서 **원호 근사(3차, κ=0.5523)** 로 바꿨습니다. 같은 반지름이라도 2차는 꼭짓점 쪽이 눌려 보여서, 사용자 눈에는 "덜 둥근" 코너로 읽힙니다.

**stack 도형 정렬** — `shapeOverflow`는 "뒷장이 오른쪽 위로 쌓인다"고 보고했는데 렌더는 앞장을 아래로 내려 그리고 있었습니다. 그래서 선택 박스·라벨·히트 영역이 전부 도형과 어긋났습니다. 앞장을 노드 박스 (0,0)에 고정하고 뒷장을 오른쪽 위로 보내 셋이 일치하게 했습니다. **도형을 새로 추가할 때 `shapeOverflow`와 실제 렌더 좌표가 맞는지 반드시 같이 확인해야 합니다** — 어긋나면 클릭 판정까지 조용히 틀어집니다.

## 2026-08-07 — AI Figure Maker 추가 (논문용 모델 구조도 에디터)

- 변경 파일: `src/tools/AiFigureMaker.tsx`, `src/tools/AiFigureMaker.css`, `src/tools/aifig/*` (11개 모듈), `src/App.tsx`, `vite.config.ts`, `README.md`
- 요약: AI/ML 논문 figure 전용 드로잉 앱을 새 도구로 추가. SVG 네이티브 렌더링 + MathJax 벡터 수식 + 논문 규격 캔버스.

### ⚠️ SVG 네이티브 렌더링 — 되돌리면 안 되는 결정

기존 도구들은 전부 HTML을 그린 뒤 `html-to-image`로 PNG를 뽑습니다. 이 도구만 캔버스를 SVG로 직접 그리고, 내보낼 때 화면에 있는 `.af-figure` `<g>`를 `XMLSerializer`로 직렬화합니다.

논문 figure는 벡터가 사실상 필수라서 PNG-only 경로는 처음부터 후보가 아니었습니다. 그리고 "SVG로 그린 뒤 그 DOM을 그대로 내보낸다"는 선택이 중요한 이유는, 렌더링 코드가 하나뿐이라 화면과 파일이 어긋날 수 없다는 점입니다. 별도의 SVG 생성기를 두는 흔한 설계는 도형 하나 고칠 때마다 두 곳을 고쳐야 하고, 반드시 한쪽이 뒤처집니다.

이 결정의 대가는 에디터 UI를 figure와 분리해야 한다는 것입니다. 그래서 선택 핸들·가이드·클릭 판정은 전부 `.af-figure` **바깥의 형제 그룹**에 있고, figure 그룹 자체는 `pointer-events="none"`입니다. 클릭은 `data-hit-node` / `data-hit-edge`를 가진 투명 hit 레이어가 받습니다. figure 안에 편집용 요소를 하나라도 넣으면 내보낸 파일에 그대로 딸려 나갑니다.

### ⚠️ LaTeX는 KaTeX가 아니라 MathJax SVG

프로젝트에 이미 KaTeX가 있지만 쓰지 않았습니다. KaTeX는 HTML+CSS로 조판하므로 SVG에 넣으려면 `<foreignObject>`가 필요한데, Illustrator·Inkscape·`\includegraphics`가 이를 무시합니다. 논문용으로는 치명적입니다.

MathJax의 SVG 출력은 진짜 `<path>` 글리프를 냅니다. `fontCache: 'none'`으로 설정해 글리프를 인라인시켰는데, 이건 파일이 조금 커지는 대신 `<use>`/`<defs>` id 충돌을 원천 차단합니다 — 수식 두 개를 한 파일에 합칠 때 id가 겹치면 조용히 깨집니다.

좌표 변환 규칙(`aifig/latex.ts`): MathJax 내부 단위는 1 em = 1000 units이고, 컨테이너 `<svg>` 안쪽 `<g>`는 이미 baseline이 y=0인 y-up 좌표계입니다. 따라서 `translate(x, baselineY) scale(fontSize/1000)`으로 감싸면 정확히 맞습니다. viewBox의 `minY`가 ascent, `height + minY`가 descent입니다.

### 함정 세 가지 (같은 자리에서 다시 밟기 쉬움)

**1. mathjax-full은 브라우저에서 그냥은 안 뜬다.** `js/components/version.js`가 자기 package.json을 읽으려고 `eval('require')`를 호출해서 `ReferenceError: require is not defined`로 죽습니다. 해결은 vite `define`으로 `PACKAGE_VERSION`을 주입하는 것 — 그 파일이 `typeof PACKAGE_VERSION === 'undefined'`일 때만 eval 분기를 타기 때문에, 상수를 정의해 두면 정적 분기로 넘어갑니다. `optimizeDeps.esbuildOptions.define`도 시도했지만 Vite 8은 rolldown을 쓰므로 deprecated 경고만 나고, 최상위 `define` 하나로 dev·build 양쪽 다 해결됩니다.

**2. MathJax 로드 완료가 구독보다 먼저 일어난다.** dynamic import가 이미 캐시된 모듈이면 microtask에서 resolve되는데, React의 passive effect는 그보다 늦게 실행됩니다. 그래서 `useEffect`에서 `onMathReady(...)`를 등록하는 시점엔 이미 "완료" 이벤트가 지나가 있고, 수식이 영원히 LaTeX 원문으로 남습니다. 콘솔에 에러도 안 납니다. `onMathReady`가 등록 시점에 이미 준비됐으면 콜백을 즉시 호출하도록 고쳤습니다.

**3. memo된 컴포넌트는 수식 캐시가 채워져도 다시 안 그린다.** `NodeView`/`EdgeView`는 `memo`인데 MathJax가 준비돼도 props가 안 바뀝니다. `rev` prop(= `mathRev`)을 넘겨 memo를 무효화합니다. 이 prop은 렌더에 쓰이지 않으니 "안 쓰는 prop"으로 보고 지우면 수식이 다시 안 나옵니다.

### 그 밖의 판단

- **캔버스 = 물리 크기.** 캔버스는 px지만 `printWidthIn`을 함께 들고 있어서, 글자 크기를 "인쇄되면 몇 pt"로 환산해 보여 주고 6 pt 미만이면 경고합니다. 논문 figure에서 가장 흔한 실수가 축소 후 글자가 안 보이는 것이라, 이 readout이 이 도구의 실질적인 핵심 기능입니다. SVG를 내보낼 때도 `width`/`height`를 mm로 적어 `\includegraphics`가 단 폭에 그대로 맞습니다.
- **팔레트는 Appendix에서 받은 관례 조합** (matplotlib tab10 / Nature muted / 그레이스케일+포인트) 에 색각 이상 안전 팔레트(Okabe–Ito, Paul Tol)를 더한 5종. 팔레트 교체는 색을 인덱스로 매핑해 바꾸므로 "어느 블록이 한 그룹인지"가 보존됩니다.
- **TikZ 내보내기는 근사입니다.** 3D 텐서·MLP·히트맵은 TikZ에 대응 도형이 없어 사각형이 됩니다. 충실한 쪽은 SVG이고 TikZ는 출발점 용도라고 README에 적어 뒀습니다. 이스케이프에 주의 — `$…$` 안의 `_`를 이스케이프하면 수식이 깨지므로 `parseLabel`로 math 구간을 구분해 텍스트만 이스케이프합니다.
- **린트**: 새 파일들은 순수 함수를 `layout.ts`·`handles.ts`·`resolve.ts`로 분리해 `react-refresh/only-export-components`를 해소했습니다. 렌더 중 ref 접근(`docRef.current = doc`) 경고는 남겨 뒀습니다 — 기존 도구 9개가 전부 같은 패턴이라 이 파일만 바꾸면 오히려 일관성이 깨집니다.
